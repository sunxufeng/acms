# ACMS 项目计划

> **这是 ACMS 项目计划与进度的单一真源（Single Source of Truth）。**
> 任何会话、任何协作者都以本文件为准，不再散落在聊天记录里。
> 最后更新：**2026-09-17** · 维护：AI 助手在每次交付后同步（新增/推进条目 → 更新状态 → 提交）
>
> 📥 **数据源约定**：本文件第二节的表格 + 同目录 `PLAN.csv`（同内容的机器可读副本），
> 即 WorkBuddy「项目 → 计划」的导入源。两者字段一致：`标题 / 状态 / 处理人 / 优先级 / 标签`。

---

## 一、项目概览

| 项 | 内容 |
|---|---|
| 项目 | **ACMS**（校务管理系统）· 致极学院 |
| 线上地址 | https://acms.areteailab.com |
| 技术栈 | pnpm monorepo：`apps/api`（NestJS 10）+ `apps/web`（Next.js 15）+ `packages/contracts`、`packages/domain`（共享契约与权限） |
| 数据层 | **PostgreSQL 宽表**（每表 `id` + `data jsonb`），已从飞书 Base 全量切读；飞书仅剩 IM / 通讯录 / 日历 / 任务 |
| 部署 | 阿里云 114.215.186.106 · **Blue-Green 零停机**（API 3001/3002 ↔ Web 3101/3102），nginx 按 `.deploy_slot` 切换 |
| 当前状态 | 全部 55 项计划中 **51 项已完成上线**（考试与成绩与身份模拟的 Phase 2 均已上线、知识库改造已上线、角色管理重构与权限判据收口已上线、会议纪要可见范围已上线），4 项待排期 |

---

## 二、项目计划总表

> **本节表格即「计划」的导入源**：列与 WorkBuddy「项目 → 计划」的字段一一对应
> （标题 / 状态 / 处理人 / 优先级 / 标签）。状态取值：`已完成` / `进行中` / `待开始`；
> 优先级：`P0`（核心或阻塞）/ `P1`（重要）/ `P2`（优化）。
> **处理人**统一留空 —— 按实际分工指派（涉及资源排期，需与负责人确认）。

### 教学管理

| 标题 | 状态 | 处理人 | 优先级 | 标签 |
|---|---|---|---|---|
| 教学域数据层（21 张表 + 4 张配置表 + 权限 + 菜单） | 已完成 | | P0 | 教学管理 |
| 成绩册（列权重 × 类型权重 + 二维录入 + 等级达标） | 已完成 | | P0 | 教学管理 |
| 课程规划（单元 / 环节 / 开课 / 部署到课次） | 已完成 | | P0 | 教学管理 |
| 行为记录（记录 / 跟进 + 告警重算 + 家长信件） | 已完成 | | P0 | 教学管理 |
| 课时教案（挂课次 + 学生 / 家长可见性） | 已完成 | | P1 | 教学管理 |
| 学习成果库（单元 / 课时挂成果） | 已完成 | | P1 | 教学管理 |
| 考勤码配置（可配置出勤口径） | 已完成 | | P1 | 教学管理 |
| 出勤率报表 + 考勤终态审核（教务审核 → 计入出勤率与结算基数） | 已完成 | | P1 | 教学管理 |
| 作业与成绩册联动（作业完成情况一键写入成绩册） | 已完成 | | P2 | 教学管理 |
| 家长 / 学生门户（多子女 + 自助 + 可见性查询层过滤） | 待开始 | | P0 | 教学管理 |
| 教学班数据补录（阻塞课程部署） | 待开始 | | P0 | 教学管理 |

### 招生管理

| 标题 | 状态 | 处理人 | 优先级 | 标签 |
|---|---|---|---|---|
| 卫瓴联系人管理（同步 + 只读列表 + 关联学生） | 已完成 | | P0 | 招生管理 |
| 招生跟进改用联系人（新建 / 修改从联系人取数） | 已完成 | | P1 | 招生管理 |
| 招生分析报表（8 个分析区块 + 6 个 KPI 下钻） | 已完成 | | P1 | 招生管理 |
| 联系人去重报表（分组 + 证据 + 建议保留 + 导出） | 已完成 | | P1 | 招生管理 |
| 候选人流失分析（按渠道 / 归属人） | 已完成 | | P2 | 招生管理 |
| 邮件自动归档（IMAP + 附件本地化 + 关联学生） | 已完成 | | P0 | 招生管理 |
| 邮件归档按用户隔离：账户「关联用户」+ 行级数据范围 + 「用户/关联」列 + 归属迁移 | 已完成 | | P0 | 权限安全 |
| 新建账号默认选中真实校区 + 校区改必填（堵 ABAC 校区两个反向坑，7 个账号曾看不到学生） | 已完成 | | P0 | 权限安全 |
| 用户表姓名与飞书对齐（宋琼｜Sally / 吴倩｜Erin / 孙旭峰｜Richard） | 已完成 | | P1 | 数据一致性 |
| 部门成员同步漏抓「根部门（公司）」直属成员，刘玉蓉｜Yvonne 查不到 | 已完成 | | P1 | 组织管理 |
| 用户管理页左侧「部门架构」树：点部门右侧账号列表同步筛选（含下级可切、可分享 URL） | 已完成 | | P0 | 权限安全 |
| 学生档案行级数据范围：角色级「数据范围」+ 人级「学生档案范围」+ 9 个学生模块接入 + 学生全景受限 | 已完成 | | P0 | 权限安全 |
| 学生档案列表/导出补齐字段级脱敏（此前只有详情页脱敏） | 已完成 | | P1 | 权限安全 |
| 角色管理「复制角色」：以现有角色为模板新建（继承权限/菜单/密级/数据范围，不继承内置与锁定） | 已完成 | | P0 | 权限安全 |

### AI 路由

| 标题 | 状态 | 处理人 | 优先级 | 标签 |
|---|---|---|---|---|
| AI 路由六模块（分组 / 上游账号 / 模型路由 / API 密钥 / 用量 / 审计） | 已完成 | | P0 | AI 路由 |
| AI 网关真实调用打通（DeepSeek 实测 + 计费 + 冷却恢复） | 已完成 | | P0 | AI 路由 |
| AI 路由用户管理（方案待定：复用 or 新建） | 待开始 | | P2 | AI 路由 |

### 报表管理

| 标题 | 状态 | 处理人 | 优先级 | 标签 |
|---|---|---|---|---|
| 学生类报表 4 张（结构概览 / 年级流向 / 入学趋势 / 档案完整度） | 已完成 | | P1 | 报表管理 |
| 笔记统计 / 活跃时段报表 | 已完成 | | P2 | 报表管理 |
| 报表下钻（点人数进名单，带筛选） | 已完成 | | P1 | 报表管理 |

### 权限安全

| 标题 | 状态 | 处理人 | 优先级 | 标签 |
|---|---|---|---|---|
| 菜单级权限点补齐 + 存量角色迁移 | 已完成 | | P0 | 权限安全 |
| 菜单入口口径收口：`read` 蕴含 `enter`（修 9 个角色「有权限却看不到菜单」） | 已完成 | | P0 | 权限安全 |
| 菜单 key 与模块资源 key 对齐（4 处别名收口）+ 合并重复的部门管理资源 | 已完成 | | P1 | 权限安全 |
| 「教务」调整冲销：菜单可见但接口 403 | 已完成 | | P1 | 权限安全 |
| 部门同步接口收权限：`POST /departments/sync` 原本任何登录用户可调 → 收口到 `module:departmentManagement:update` | 已完成 | | P0 | 权限安全 |
| 修复菜单 key 与模块资源 key 不一致导致 4 个菜单静默隐藏（回归修复） | 已完成 | | P0 | 权限安全 |
| 修复权限矩阵保存后管理员被降级回历史快照（自愈缺口） | 已完成 | | P0 | 权限安全 |
| 字段密级脱敏（FIELD_LEVELS 自动脱敏） | 已完成 | | P0 | 权限安全 |
| 审计四件套（创建 / 更新人 + 时间，79 张表） | 已完成 | | P1 | 权限安全 |
| 操作日志 / 登录日志 / 系统监控 | 已完成 | | P2 | 权限安全 |
| 身份模拟（系统管理员二次密码解锁后，可以任意账号身份浏览 ACMS，进入/退出全程留痕） | 已完成 | | P1 | 权限安全 |
| 会议纪要可见范围（公开 / 部门内可见 / 指定部门可见 / 指定用户可见 / 仅自己可见 + 部门负责人跨部门可见） | 已完成 | | P0 | 权限安全 |

### 基础设施

| 标题 | 状态 | 处理人 | 优先级 | 标签 |
|---|---|---|---|---|
| 飞书 → PostgreSQL 数据层迁移（79 表全量切读） | 已完成 | | P0 | 基础设施 |
| Blue-Green 零停机部署 | 已完成 | | P0 | 基础设施 |
| 云盘 / 云文档 / 字典内化 | 已完成 | | P1 | 基础设施 |
| 部门管理（部门树 + 部门成员） | 已完成 | | P2 | 基础设施 |
| 外部客户端接入（CLI / MCP / API 令牌，让 agent 调用 ACMS） | 已完成 | | P1 | 基础设施 |
| 全站按钮权限收口（CrudPage `moduleKey` 全覆盖 + 只对已声明动作生效） | 已完成 | | P0 | 权限安全 |
| 知识库配置按用户隔离（「关联用户」多选 + 可见性判据唯一化） | 已完成 | | P0 | 权限安全 |
| 笔记正文落库（`noteBody`：智能总结与原始记录分列） | 已完成 | | P1 | 基础设施 |
| 「重新收取」笔记正文（异步 + 进度 + 幂等 upsert） | 已完成 | | P1 | 基础设施 |
| 角色管理页重构（菜单可见性并入矩阵 + 列表视图按模块罗列） | 已完成 | | P0 | 权限安全 |
| 权限判据双轨制收口（12 处自建 controller 切到 `module:` 体系 + 菜单白名单修复） | 已完成 | | P0 | 权限安全 |

### 数据质量

| 标题 | 状态 | 处理人 | 优先级 | 标签 |
|---|---|---|---|---|
| 首页 logo 旧附件待人工重传 | 待开始 | | P2 | 数据质量 |

## 三、待办与阻塞（4 项）

> 每项都有对应的 GitHub Issue（WorkBuddy「项目 → 计划」读的就是 Issues，两处保持一一对应）。
> 状态变更时**两处一起改**：这里改表格、issue 改 state。

| 优先级 | 标题 | Issue | 依赖 / 说明 |
|---|---|---|---|
| **P0** | 教学班数据补录 | [#1](https://github.com/sunxufeng/acms/issues/1) | **阻塞其他项**：教学班表为空，课程规划的「部署环节到课次」无法验收 |
| **P0** | 家长 / 学生门户 | [#2](https://github.com/sunxufeng/acms/issues/2) | 需要消费已就绪的可见性开关（学生/家长两个正交开关 + 成绩完成闸门） |
| **P2** | AI 路由用户管理 | [#3](https://github.com/sunxufeng/acms/issues/3) | 需先定方案（复用 vs 新建），再排期 |
| **P2** | 首页 logo 重传 | [#4](https://github.com/sunxufeng/acms/issues/4) | 纯人工操作 |

### 上线后待人工（**不属于计划条目**，不计入 54 项统计）

> 这些是「代码已就绪、但需要人在界面上点几下才生效」的初始化动作。
> 不记录在第二节总表里，避免污染计划条目数；做完可逐条划掉。

| 事项 | 在哪做 / 说明 |
|---|---|
| 配「考核类型」 | 教学管理 › 考核类型 —— 名称 / 颜色 / 缺省权重 / 是否计入总评。成绩册列头色块与第二层权重缺省都取这里 |
| 建「成绩批次」 | 成绩批次 —— 学年 + 学期 + 日期区间 + 舍入 / 免考 / 缺考口径。**没有批次就没有期末总评** |
| 给成绩等级配「绩点」 | 成绩等级 —— 不配绩点则 GPA 与班级排名为空（页面会明说「未配置绩点」，不会显示 0.00） |
| 给存量角色补授权 | 角色管理 › 权限矩阵 —— 矩阵存的是**已派生的完整权限集**，新模块不会自动补：`module:examGrades:*` / `module:examTypes:*` / `module:examComments:*`（管理员由 `healLockedRoles()` 兜底） |
| 配 `IMPERSONATE_PASSWORD`（可选） | 生产环境变量 —— 配了则以它为准，代码内默认值失效。不配也能用，但默认值写在源码里，只防误操作不防攻击。**同一个密码管「身份模拟」与「令牌管理」两个闸**（实测生产当前 `passwordSource=default`） |
| 签发第一条真实令牌 | 后台管理 › 令牌管理 —— 系统管理员 + 二次密码。建议**按用途分开签**（只读给查询、可写给自动化），不用了立刻吊销 |

> ✅ 2026-09-14 已消解：出勤率报表（含考勤终态审核）、作业与成绩册联动 —— 见下方里程碑。
> ⚠️ **考勤数据尚未积累**：生产「学生考勤记录表」当前 0 行，出勤率报表数字全为 0 属预期；
> 有打卡/补录数据后自动出图。另：考勤码表为空时按内置默认 7 码计算。

---

## 四、已完成里程碑

### 2026-09-18
- **录音全链路打通：转出带录音 + 列表行内播放 + 容器按文件头嗅探**（`6b7186b`）：
  峰哥报 4 件事，逐条落地 ——
  ①「部分音频没法播放」根因 = 录音落库时 MIME 与扩展名**写死 `audio/ogg`**，而实测
  554 个录音里 **40 个实际是 MP3**（另有 29 个 m4a / 1 个 webm）；浏览器按 `audio/ogg`
  解 MP3 字节流直接解码失败。新增 `file-storage/audio-format.ts` **按文件头嗅探**真实容器，
  播放接口动态给 Content-Type；存量 40 个文件的元数据 + 正文表展示信息用
  `scripts/fix_note_audio_media.mjs` 修正（残留 0，m4a/webm 只报告不改 —— `ftyp` 分不出
  音频轨与视频轨，硬改会让真视频丢画面）。
  ② 播放链路补 **Range（206）** 与音频 `inline`（新 `file-storage/byte-range.ts`）：
  录音最长 3 小时 / 单文件 168 MB，全量返流会让进度条拖不动、**Safari 直接拒播**。
  ③ 「我的笔记」**列表接口返回每行 `_audio`**（主键批量查，不打上游、不受 QPS 2 限制），
  `CrudPage` 新增 `rowActionSlot` 插槽，操作列放「▶ 播放 / ⏸ 停止」（单 Audio 实例、
  同一时刻只播一条；录音类但未落库的显示禁用按钮 + 原因提示）。
  ④ **转出时带上录音**：`NoteConvertTarget` 新增 `audioField`（11 个模块默认映射），
  会议纪要新增「会议附件」列承接，转换配置页可编辑；附件字段里音频渲染内联播放器。
  **历史数据**：已转出的 5 条业务记录用 `scripts/backfill_convert_audio.mjs` 补上录音
  （token 复用、不复制文件）；另修**正文表归属缺失**（66 行 `来源配置ID` 为空，42 行从
  快照表回填）—— 这正是 8 条贺嫣的笔记补抓音频报「权限不足」的根因（凭证解析退化成
  管理员自己的 Key ⇒ 上游 10008）。
  **结果**：音频 533 → **546 条 / 312.7 小时**，待抓 0；峰哥点名的贺嫣「IDP一对一沟通：
  个人情况、职业规划与在校学习安排」（19.8 分钟）与蒋潘云「付庄浩Preto升学沟通面谈记录」
  （7.4 分钟）均已补齐。
- **数据范围「一个都不选」由「看全部」纠正为 fail-closed**（`138143e`）：原语义
  fail-open，配错角色等于全放开；引入 `SCOPE_DENY_ALL` 标记，`studentInScope` 恒 false，
  10 处调用点零改动。实测 Phase1 82 / 只配 Pre-1 → 33 / 空范围 → 0 条。
- **「数据密级」配置页上线**（`55dd88e`）：字段级密级的唯一配置入口（adminOnly），
  含用户密级分布 / 按模块分组的字段密级矩阵 / 保存前 diff / 打码预览；
  可配模块 4 → 18 个（`FIELD_LEVEL_MODULES` 刻意放 `dict.data.ts` 而非
  `module-permissions.ts`，避免把 DB 表 id 泄到浏览器 bundle）。
- **角色页保存按钮点不动 + 「密级上限」长期不生效的误导**（`68874aa`）：
  `dirty` 手写比较漏了 `dataScope`；`auth.service` 把空密级写成 `'L1'` 导致个人值永远有值、
  角色上限的回退分支永远走不到 —— 抽出 `engineLevelOfUserField()`（空 ⇒ undefined 回退角色、
  已知值 ⇒ 覆盖、无法识别 ⇒ L1 保守处理）。
- **`/data-levels` 页面不停闪**（`3bf8a04`）：`useTl()` 每次 render 返回新函数，而页面写了
  `useEffect(..., [tl])` ⇒ 死循环（服务端铁证：同秒最多 49 次 `field-levels/catalog`）。
  改为 `useCallback([locale])` + `useRef` 持最新 labels；同类写法生产共 6 处一并核对。

### 2026-09-17
- **按钮权限全站收口 + 知识库（笔记）三项改造**（`9bc6a9a` / `5c86259` / `05ff828`，Issue #8）：
  峰哥报障「曹德强没有知识库设置 / 我的笔记的编辑删除权限，却仍看得到按钮」——
  查下来**不是权限配错，是全站 59 处 `CrudPage` 里有 33 处没传 `moduleKey`**，
  而 `modOk` 在没传时直接返回 true（= 完全不控），这些页面对所有角色都显示新建/编辑/删除，
  点下去才 403。已给 32 处补上（剩 `/venues` 是孤儿路由，无菜单无入口，留待决定是否下线），
  并把 `modOk` 改成「只对模块 `actions` 里确实声明过的动作生效」，避免误藏按钮
  （`aiAgents` / `getnote` / `users` 的 actions 不含 export，硬判会让导出对所有人消失）。
  **安全性依据**：后端 `require(user, action)` 用的也是 `module:<key>:<action>` 同一权限点，
  所以「按钮隐藏 ⇔ 接口 403」天然一致。另三项：**笔记正文落库**（新表 `noteBody`，
  总结与原始记录分列存；与快照表分表，避免列表查询多拖几 MB）、**「重新收取」**
  （`POST /getnote/refetch-bodies`，异步 + 进度轮询 + 按笔记 ID 幂等）、
  **知识库配置改多用户关联**（新增「关联用户」字段，可见性判据抽成唯一一处
  `sourceVisibleTo()`，列表 / 详情 / 写鉴权 / 笔记收窄全调它）。
  生产实测：管理员见 17 条配置 / 曹德强只 1 条、越权读写均 403、我的笔记 482 vs 105、
  重新收取 9 条 stored=9 failed=0、9 个页面回归全 200。
  ⚠️ 部署时踩到一条重要事实：**生产 `SQL_TABLES=*`，所有表都路由到 PG，飞书 Base 只是历史镜像**
  —— 字段元数据必须写进 `acms_fields`、数据必须写回 `t_*.data` jsonb，改飞书对运行时零影响。
- **知识库两页修补：配置页分页回归 + 四处筛选**（`0c374b6`）：峰哥报「配置页只显示 10 条」
  ——真因是**上一轮把可见性过滤放进 `list()` 的后置步骤**，过滤在分页之后只能把结果收敛成单页
  （`total = 本页条数`、`hasMore = false`）⇒ 前端分页条永远 1 页、第 2 页起的数据翻不到。
  改为在 `RecordMeta` 上声明 `rowScope`（条件进 SQL WHERE），total/分页恢复准确。
  同批：配置页去掉「归属人」筛选、加「配置名称」（模糊）与「用户」（`关联用户__has`）筛选，
  `pageSize` 调到 50；「我的笔记」补 `来源`/`配置名称`/`归属人`/`标签` 四个服务端筛选
  （此前这些筛选控件发出去的参数被接口静默忽略，看着有 UI、点了没反应），
  并把「来源/标签」的拆分口径抽到 `packages/contracts`（前端展示与后端筛选共用一份）。
- **修复「我的笔记」列表恒为空**（`5c58639`，Issue #11）：峰哥报「刘佳音｜Joy 登录后我的笔记
  没有记录，系统管理员能看到她的笔记」。根因是**本人凭证源被可见源白名单丢弃**——
  她在向导页填过个人凭证、且与自己的配置是**同一份 API Key**，`collectAllNotes` 按 Key 去重时
  个人凭证源先占住 `seenKey` ⇒ 配置源被跳过 ⇒ 笔记全挂在 `_sourceRecordId=''` 上 ⇒
  严格白名单（`Set.has`）一个都不命中 ⇒ 恒为空；管理员因 Key 不同不受影响，故表现为
  「自己看不到、管理员看得到」。判据抽成纯函数 `noteInScopedSources()`（**空 recordId 必须放行**，
  它是本人凭证源、只含自己的笔记），并顺带让非管理员**只拉「我可见的源」**——
  原来每进一次页面要打完 12 个启用源（24 秒，且消耗**所有同事**的上游额度）。
  生产复验：刘佳音 **0 → 50 条、24s → 3.4s**，其余 11 人逐个实测全部正常（无一人为 0），
  管理员 575 条不变；新增 13 项契约测试，全量 api 128 + domain 17 全过。
  顺手修 `matchFilter` 漏认 `<字段>__link` 的既有隐患。线上实测：配置页 `pageSize=10 → total=16/hasMore=true`
  且能翻页；Elsa/Rin/Ruby 各只看到自己那 1 条配置与其笔记（25/47/6），标签筛选 12/2/0。
- **角色管理菜单顺序对齐系统菜单**（`129fd58`）：矩阵/列表视图的菜单分组与排序改为**与侧边栏同源**
  —— 原先按 `section` 首次出现顺序分组、且完全不按 `order` 排序，导致教学管理/AI 路由整组排到最后、
  分组名与侧边栏不一致（`知识库` vs `知识笔记`）。新增 `buildSystemSections()` 复刻 `AppShell.tsx`
  的分组算法（menu-groups 定分组与顺序、`order` 定组内顺序、`section` 同时比 key 与 label、
  未登记 section 兜底成组），矩阵/列表/白名单折叠区三处共用；adminOnly 18 项统一收进末尾
  「系统管理员专属」分区。用生产数据离线核对（抠源码真函数 + 11 类断言）与线上浏览器双向验证均一致。
- **笔记数据全量落库 + 报表口径与两个新维度**（`6be329f`）：峰哥报六项，逐一用生产实测坐实
  —— ①「配置名显示 1234567 / 看不到某人笔记」同一根因：映射表 88 行挂在**已删除的配置**上、
  157 行重复，显示层解析不到就**回退历史名**，而筛选下拉只列当前配置名 ⇒ 一条都筛不出来
  （Angela 的 19 条其实一直在列表里）。修法：映射主键改**笔记ID**（upsert 幂等）+
  一次性清理（688 → 580 行，配置名刷新为当前值，原表备份在 `bak_notemap_20260917`）。
  ② 字段落库：快照表补 标签/标签类型/归属人ID/笔记类型/来源/总结/子笔记数
  （原仅 8 字段、`acms_fields` 0 条元数据），正文表补 标签/来源/录音时长/附件数/录音卡SN ——
  实测上游**列表接口就返回总结与标签**，这两样零额外额度；570 条正文一次性补齐（QPS 2）。
  ③ 报表「按人/按来源」改为**按 ID 归并 + 名字实时解析**（改名残留会把同一配置拆两行）。
  ④ 新增「笔记统计 · 按标签」（多标签分别计入、排除来源标签、system 标签默认隐藏）
  与「招生分析 · ④ 按状态」（1 已认领 / 4 待分配 / 0 待认领（公海），含状态×阶段分布与下钻）。
- **角色管理页重构：「菜单可见性」并入矩阵 + 列表视图按模块罗列**（`1368b73`，Issue #8 后续）：
  先量化确认重叠 —— 菜单 76 项与模块 71 个**一一对应**（67 同名 + 4 走 `MENU_KEY_ALIASES`），
  矩阵的 `enter` 列恰好全覆盖；无对应模块的 5 项全是 `adminOnly` 或走独立权限点
  （`system-monitor`→`admin:monitor`、`aiDocs`→`ai:chat`），不必造伪模块。
  矩阵的行改为**菜单项**（按菜单分组分区），«进入菜单»列按三类行分别落到 `module:<key>:enter` /
  独立权限点 / 「—（仅管理员可见）」；删除独立的「菜单可见性」表格，但**「菜单白名单」不删**
  —— 它与 `enter` 不是同一层（前者是额外收敛，且「只有该角色所有角色都配了白名单才生效」），
  收进折叠区零行为变化。另定位列表视图的真病因：其渲染里把 `module:*` **全过滤掉**，
  与矩阵**零重叠**（用户说的「不完整、对不上」完全准确），改为按模块罗列 + 底部「兼容权限点」折叠区。
- **权限判据双轨制收口（收回越权）+ 菜单白名单修复**（`fd1f306`，Issue #9）：峰哥报「曹德强没有学生档案
  修改/删除权限却能看到按钮」+「Angela/丁懿 只能看到报表」。查下来是**同一类结构缺陷**：
  系统并存两套判据 —— `module:<key>:<action>`（角色管理矩阵编辑的、通用 CRUD 后端也用的）
  与 legacy（`student:write`/`getnote:write`…，只有少数**自建 controller** 在用），
  而矩阵只能编辑前者 ⇒ 界面与实效脱节。用**零副作用**请求实测坐实：曹德强(Phase1) 新建/编辑/删除
  学生档案分别返回 400/400/404（= 权限检查已通过，只因 payload 空被业务校验拦下），
  对照「教师本人」三项全 403 —— 即峰哥当初在矩阵里取消勾选，**权限根本没被收回**。
  已把 12 处自建 controller 的判据统一切到 module（student / getnote / weiling / dashboard /
  student-360 / reports / mail-archive / partnership / attendance / export / monitor / user /
  wechat-binding），前端 `/students` 补按钮门控（此前该页**一个权限判断都没有**）。
  菜单侧：Phase2/3 的白名单只有 3 项且其中 2 项缺 enter（白配）⇒ 把 24/23 个已授权菜单卡成 1 个，
  已清空白名单回自动模式；Phase1 白名单里 2 项缺 enter 的已补权限。
  线上实测：曹德强三项写全 403、丁懿/贺嫣恢复正常；菜单可见数 曹德强 9→11、丁懿 1→24、贺嫣 1→23。
- **会议纪要可见范围权限**（`2f5f471`，Issue #10）：会议纪要从「有模块权限就能看全部」
  改为按创建时选择的可见范围做行级隔离（**公开 / 部门内可见 / 指定用户可见 / 仅自己可见**），
  并支持「**部门领导看下属所有部门**」。判据挂在 `RecordMeta.rowScope`（覆盖 list / detail /
  export / 内存深筛；update / archive 因先调 detail 而自动受保护），豁免角色
  `系统管理员` + `院级管理`（= 公司最高领导人）。
  **「部门领导看下属部门」没有单独分支** —— 它被吸收进「我的部门范围」＝
  （我所属部门 ∪ 我担任负责人的部门）各自展开子树，负责人只是拥有更大的部门集合；
  部门负责人取自部门表 `leader_user_id`（实测存的是 **openId**，可直接与会话比对，无需映射）。
  ⚠️ 三条硬约束（已回写技能 `acms-row-scope` 与 memory）：
  ① **空 `value` 数组 = 不过滤 = 全部放行**（引擎两侧都把空数组当「没有条件」：
  `matchFilter` 返回 true、`buildCondition` 返回空串）⇒ 运行时算出的集合为空时，
  **整条 OR 分支必须不加入**，否则「部门内可见」会静默退化成「全员可见」；
  ② **`rowScope` 只能过滤 jsonb 字段**（条件一律 `data ->> $n`），**物理列 `created_by` 参与不了**
  ⇒「仅自己可见」必须把创建人冗余成 jsonb 字段「创建人ID」（create 钩子自动写；创建人不再变，
  所以「update 没有钩子」这个限制对它不成立）；
  ③ 飞书对**根部门**结构性地不返回负责人（`/contact/v3/departments/0` 的返回体**连 leader 键都没有**，
  且 name 为空串）⇒「公司最高领导人」只能用角色判，**不能用根部门负责人**
  （实际宋琼｜Sally 本身就是系统管理员，需求已被满足）。
  上线当天实测发现并修复：**创建人看不到自己创建的「指定用户可见」记录**
  （且 `create()` 写后回读 `detail()` 被自身判据挡住 ⇒ POST 返 **404**、前端误报「保存失败」
  而数据其实已写入，用户会重复提交）—— 修复为把「创建人 = 我」提升成**独立于可见范围**的顶层 OR 分支。
  验证：探针会话 4 条 × 4 身份全绿（越权详情 404 / 指定用户 200）、单测 22 项、i18n 校验、
  8 个页面回归 200，测试数据残留 0；**零引擎改动**。
  同批还顺手刷新了组织数据（刘玉蓉｜Yvonne 按飞书调整归入院长办公室 —— 她原先直属「公司」根部门，
  会让她的部门范围等于全部 9 个部门）。

- **会议纪要可见范围增补：新增「指定部门可见」+ 参会/缺席/列席改多选**（`00f9624`，Issue #10）：
  ① 可见范围从 4 档扩到 **5 档**，新增「**指定部门可见**」—— 可多选部门，**选中部门（含其下级）
  里的人都能看到**；新建时**默认选中「我所属的部门」**（新增 `GET /departments/my-departments`，
  以及 CrudPage 的通用 `createDefaults` 能力）。
  判据用 `open_department_id` 做 `contains`（**不用部门名** —— 多值字段只能子串匹配，
  部门名会串台（「教学」命中「教学管理中心」）；用 id 还顺带免疫部门改名）。
  **「我能命中的部门」= 我的部门 ∪ 其上级**，这样把纪要指定给上级部门时**整棵子树**都看得到。
  🔴 根部门「公司」的 `open_department_id` 是**字符串 `'0'`**，拿它做子串匹配会命中一切 ⇒
  从选项到判据**全链排除**（要全公司可见就用「公开」这一档）。
  ② 参会 / 缺席 / 列席人员改为**多选**（`person + linkMulti`，存姓名数组，与单选的「主持人」同口径）；
  CrudPage 顺带支持 `person + linkMulti` 与 `linkSource: 'departments'`。
  ③ 🔴 踩坑回写：三条人员字段**不能**登记进 `linkFields` —— 那个分支优先于 `multi`，
  会把数组读成「张三、李四」拼接字符串（实测 detail 直接返回字符串，前端回显与断言全跟着错）。
  验证：单测 29 项（+7）、全量 132 项、线上 4 身份可见性矩阵 **4/4**（含跨上下级）、页面回归 8/8、
  测试数据残留 0。
  ⚠️ 部署时又踩到「**打包失败但部署照跑**」：用分号串联时 `build_tars.sh` 被批量删除守卫拦下、
  tar 没重建，而 deploy 照跑、把**上一轮**构建发上了线（线上 buildId 与本地不一致，
  但部署脚本打印的「期望/实际」两边都是旧的，看起来一切正常）。
  已改为 `&&` 短路（BUILD_ID 不一致就不部署），并把这条与「scope: turn 下 export 也压不住守卫、
  要 `mv` 走 rm 目标」一起写进技能 `acms-prod-deploy`。

### 2026-09-16
- **令牌管理页缺陷修复**（`167edd1`）：峰哥截图报障「签发弹窗没有关闭 ×／到期日选一年报错」。
  根因是**页面默认值本身就是非法值** —— 日期框只有「日」精度、提交时按当天 23:59:59 算，
  而服务端上限写的是 `now + 365×24h`，差几个小时 ⇒ 用户什么都不改直接点「确认签发」也会被拒，
  整个签发功能不可用。修法：上限改按**日历**算（`now+365天` 那天的 23:59:59.999），
  抽成纯函数 `auth/api-token-expiry.ts` 并加 5 个回归单测；通过 `/state` 把绝对上限下发前端
  （绑 `input[type=date]` 的 max）+ 提交前本地前置校验。同批补上弹窗关闭 ×（含 Esc）、
  并修掉 7 个 i18n 漏 key —— 其中 3 个是令牌页自己的（界面直接显示 `apiTokens.cancel` 这类原始 key），
  另 4 个（`markbook.hwBindDone`、`common.{ok,failed,notSupported}`）是新检查扫全站揪出来的**同类线上缺陷**。
  `i18n_labels_lint.mjs` 已补上「`useTranslations(ns) + t(key)` 的 key 是否存在」这条检查，
  此前它只查 `tl()` 字面量，所以这类漏 key 能一路绿灯上生产
- **外部客户端接入上线**（`0b5cda5`，Issue #7）：让 WorkBuddy / Codex 等 agent 能调用 ACMS，
  不必再让人把浏览器 Cookie 抠出来塞进脚本 —— 新增 **API 令牌**（`acms-sk-` + 43 字符 base64url，
  记录 id = 明文 SHA-256，库里不存明文，明文只在签发响应里出现一次；默认只读 + 模块白名单 +
  IP 白名单 + 限流 + 过期 + 全量留痕可选）、**令牌管理页** `/api-tokens`（adminOnly +
  二次密码，与身份模拟同门禁）、**CLI / MCP**（`apps/cli/`，零依赖纯 ESM，经
  `apps/web/public/cli/` 分发 ⇒ 免改 nginx）、**`acms schema` 能力发现**（仓库无 OpenAPI，
  48 模块 / 338 路由，另留通用透传）。关键设计：令牌鉴权搭在 `SessionGuard` 里加分支而
  **不另起守卫** ⇒ `req.user` 仍是标准 `SessionUser`，学生行级数据范围 / `requireModule()` /
  审计操作人 / `currentActor()` 零改动自动生效；`impersonation-limit.ts` 提为通用
  `access-limit.ts`，靠 `kind` 区分「模拟对 `/impersonate/*` 永久放行」与「令牌硬拒」。
  生产实测：8 条硬拒路径全 403、只读写操作 403 `TOKEN_READONLY`、吊销后立刻 `TOKEN_DISABLED`
  （主动清缓存）、`curl -fsSL .../cli/install.sh | sh` 可用；81 个单测 + i18n lint 全绿
- **考试与成绩上线**（`3c87e4c`，Issue #5）：在既有 Gibbon 式成绩册之上补「结果产出」闭环 ——
  考核类型配置 → 成绩册录入增强（科目/列头色块/单元格状态/宽容输入解析）→ 期末总评结转
  （自归一化分母、免考不进分母、缺考按 0 进）→ 各科教师评语（批量页失焦即存）→ 成绩单 + PDF 导出；
  另含异常成绩审查（5 条规则，只提示不改分）与竞赛排名 / GPA。成绩单 PDF 走 pdfkit + 子集化中文字体，
  实测约 34 KB/份、服务器零系统依赖；新增 4 张表、15 个权限点
- **身份模拟上线**（`c98911c`）：系统管理员在 `/impersonate` 用二次密码（默认值可在
  `IMPERSONATE_PASSWORD` 覆盖）解锁后，可列出系统全部账号并以任意账号身份浏览 ACMS（排障用），
  顶部常驻金色横幅提示「以谁的身份、谁发起、剩余多久」、一键退出回管理员；
  四道防线 = 登录态 + 系统管理员硬校验 / 二次密码 / 同 IP 5 次锁 15 分钟 / 进入退出全程留痕；
  新增「身份模拟记录表」，并修掉两个隐患（模拟写登录日志污染活跃统计、覆盖目标用户 openid 反向索引）
- **考试与成绩 Phase 2 上线**（`eb3a423`）：`/reports` 新增「考试成绩分布」与「GPA 与班级排名」
  两张报表（**套学生行级数据范围**，含姓名分数明细不给越权看）；「成绩口径设置」做成
  模块第 5 个 Tab（复用系统配置表，批次字段优先的三级回落）；新增「常用评语库」表 + 页面
  （批量评语可一键追加）；**整班成绩单 ZIP 导出**用零依赖手写 zip 实现（生产不能装新依赖）；
  学生全景加「期末总评 / 成绩单」两个分区
- **身份模拟 Phase 2 上线**（同 commit）：**只读模式**与**模块白名单**（写进会话，
  由 SessionGuard 统一拦截，业务接口零改动；`/impersonate/*` 永远放行否则退不出去）；
  新增「模拟记录」页（只读审计，不需要二次密码）
- ⚠️ **一次部署事故**：`build_tars.sh` 的批量删除被沙箱守卫拦截 → tar 未重生成 →
  部署上传旧 tar，而 `| tail` 吃掉了退出码，表面"成功"实则生产仍是旧版。
  判据 = BUILD_ID 与本地 `.next/BUILD_ID` 不一致。之后部署都要核对这一项

### 2026-09-14
- **考勤终态审核上线**：学生考勤记录「待审核 → 已通过 / 已驳回」（单条 + 批量，驳回必须填理由），
  权限点 `attendance:approve`（系统管理员 / 院级管理 / 教务）；审核人 / 审核时间 / 审核意见落库并写审计日志
- **出勤率报表上线**（`/reports` 第 8 张报表「考勤分析」）：应出勤人次 / 实到 / 出勤率 / 迟到 / 请假 /
  缺勤 / 异常，按班级 · 年级 · 学生排行 · 按日/周趋势，KPI 可下钻名单；
  **只统计「已通过」终态记录**，未审核与已驳回单独计数不进分子分母；
  「计入统计=否」的考勤码整条排除；码表认不出的码单独计数（不猜）
- **作业 → 成绩册联动上线**：选班 → 选作业 → 预览 → 同步（未完成 / 无提交**留空不写 0**，
  得分优先取已批改的提交分数、无分数按满分折算）；列 ↔ 作业用列上「关联作业」自然键绑定，
  列头显示完成率；预览与写入共用同一份计划器，二次同步幂等（跳过已存在）
- **联系人去重报表上线**（`00a518b`）：50 组 / 109 条疑似重复，支持分组证据、建议保留、导出 CSV
- 修复通用 CRUD 文本筛选一律等值匹配的问题（`1792e25`）—— 全站 8 处文本筛选恢复模糊匹配
- **修复权限自愈缺口**：系统管理员权限在「保存角色矩阵」后被降级回历史快照（382/498），导致联系人菜单消失、招生分析 403 —— 在 `applyToEngine` / `persist` 收口处自愈（`cf9d5b0`）

### 2026-09-13
- **教学域三模块上线**（`73b09a0`）：成绩册 / 课程规划 / 行为记录（21 张新表）
- **14 个菜单补齐权限点 + 存量角色迁移**（`0f1c036`）—— 教学管理 / AI 路由菜单首次可授权
- 部门管理改造：修「公司」根节点不显示、新增部门成员快照（点部门看员工）
- 修复 `SqlStore.search` 返回 `recordId` 的通用陷阱（`2a3a1d9`）—— 影响 3 处
- 卫瓴联系人：筛选下拉改取实际值（不再显示数字编码）、列表瘦身、招生分析 KPI 下钻（`41d4661`、`e05e367`）

### 2026-09-12
- **AI 路由上线**（`4e7b368`、`4697c24`）：从 acapi/sub2api 移植网关全套（6 模块 + `/v1` 转发）
- 招生分析新增流失分析维度、跟进分析；学生类报表支持下钻

### 更早（2026-08-25 ~ 09-11）
- 飞书 → PostgreSQL 数据层迁移（79 表全量切读、1838 个附件全部本地化）
- Blue-Green 零停机部署上线
- 角色权限体系（模块 × 动作矩阵 + 字段密级脱敏 + 审计四件套）
- 邮件自动归档模块（多账号 IMAP + AES 加密凭证 + 附件本地化 + 关联学生）
- 全站国际化（zh/en）+ i18n 构建期 lint
- 部门管理、系统监控、登录日志、活跃时段报表

> 完整执行记录（673 条）见 [`docs/TASKS-ARCHIVE.md`](docs/TASKS-ARCHIVE.md)。

---

## 五、统计

| 指标 | 数值 |
|---|---|
| 计划条目 | 42（已完成 38 · 待开始 4） |
| 执行任务（8/25 – 9/17） | 673（已完成 671） |
| 工作日 | 24 天 |
| 线上部署形态 | Blue-Green，当前活跃 slot 见服务器 `/opt/acms/repo/.deploy_slot` |

---

## 六、协作约定

1. **本文件是唯一真源**：计划变更（新增条目、调整优先级、推进状态）都改这里，然后提交。
2. **状态口径**：代码合并 + 部署上线 + 线上验证通过，才算 `已完成`；只写完代码未上线为 `进行中`。
3. **待办项流转**：进入开发前，把该条目在项目管理工具里建成事项、指派负责人、挂上本文件与 PRD。
4. **AI 助手同步义务**：每次交付后更新本文件的对应行（状态 / 备注里的 commit），并在 `docs/TASKS-ARCHIVE.md` 追加当日执行记录。
5. **上线纪律**：部署生产与推送 GitHub 需先确认；改动后必须线上验证。

### 相关文档
- [架构说明](docs/ARCHITECTURE.md) · [部署说明](docs/deploy.md) · [PG 迁移方案](docs/postgres-migration-plan.md)
- [学生门户方案](docs/student-portal-plan.md)（门户待办的设计底稿）
- [任务归档](docs/TASKS-ARCHIVE.md)
