# ACMS 项目计划

> **这是 ACMS 项目计划与进度的单一真源（Single Source of Truth）。**
> 任何会话、任何协作者都以本文件为准，不再散落在聊天记录里。
> 最后更新：**2026-09-17** · 维护：AI 助手在每次交付后同步（新增/推进条目 → 更新状态 → 提交）
>
> 📥 **数据源约定**：本文件第二节的表格 + 同目录 `PLAN.csv`（同内容的机器可读副本），
> 即 WorkBuddy「项目 → 计划」的导入源。两者字段一致：`标题 / 状态 / 处理人 / 优先级 / 标签`。

---

## 一、项目概览

| 项 | 内容 |
|---|---|
| 项目 | **ACMS**（校务管理系统）· 致极学院 |
| 线上地址 | https://acms.areteailab.com |
| 技术栈 | pnpm monorepo：`apps/api`（NestJS 10）+ `apps/web`（Next.js 15）+ `packages/contracts`、`packages/domain`（共享契约与权限） |
| 数据层 | **PostgreSQL 宽表**（每表 `id` + `data jsonb`），已从飞书 Base 全量切读；飞书仅剩 IM / 通讯录 / 日历 / 任务 |
| 部署 | 阿里云 114.215.186.106 · **Blue-Green 零停机**（API 3001/3002 ↔ Web 3101/3102），nginx 按 `.deploy_slot` 切换 |
| 当前状态 | 全部 55 项计划中 **51 项已完成上线**（考试与成绩与身份模拟的 Phase 2 均已上线、知识库改造已上线、角色管理重构与权限判据收口已上线、会议纪要可见范围已上线），4 项待排期 |

---

## 二、项目计划总表

> **本节表格即「计划」的导入源**：列与 WorkBuddy「项目 → 计划」的字段一一对应
> （标题 / 状态 / 处理人 / 优先级 / 标签）。状态取值：`已完成` / `进行中` / `待开始`；
> 优先级：`P0`（核心或阻塞）/ `P1`（重要）/ `P2`（优化）。
> **处理人**统一留空 —— 按实际分工指派（涉及资源排期，需与负责人确认）。

### 教学管理

| 标题 | 状态 | 处理人 | 优先级 | 标签 |
|---|---|---|---|---|
| 教学域数据层（21 张表 + 4 张配置表 + 权限 + 菜单） | 已完成 | | P0 | 教学管理 |
| 成绩册（列权重 × 类型权重 + 二维录入 + 等级达标） | 已完成 | | P0 | 教学管理 |
| 课程规划（单元 / 环节 / 开课 / 部署到课次） | 已完成 | | P0 | 教学管理 |
| 行为记录（记录 / 跟进 + 告警重算 + 家长信件） | 已完成 | | P0 | 教学管理 |
| 课时教案（挂课次 + 学生 / 家长可见性） | 已完成 | | P1 | 教学管理 |
| 学习成果库（单元 / 课时挂成果） | 已完成 | | P1 | 教学管理 |
| 考勤码配置（可配置出勤口径） | 已完成 | | P1 | 教学管理 |
| 出勤率报表 + 考勤终态审核（教务审核 → 计入出勤率与结算基数） | 已完成 | | P1 | 教学管理 |
| 作业与成绩册联动（作业完成情况一键写入成绩册） | 已完成 | | P2 | 教学管理 |
| 家长 / 学生门户（多子女 + 自助 + 可见性查询层过滤） | 已完成 | | P0 | 教学管理 |
| 成绩册「学科分行」视图：同一考核项的多学科列归并为一列（表头不再重复科目）+ 归并列的编辑按科目切换、删除按科目批量 | 已完成 | | P1 | 教学管理 |
| 教学班数据补录（阻塞课程部署） | 待开始 | | P0 | 教学管理 |

### 招生管理

| 标题 | 状态 | 处理人 | 优先级 | 标签 |
|---|---|---|---|---|
| 卫瓴联系人管理（同步 + 只读列表 + 关联学生） | 已完成 | | P0 | 招生管理 |
| 新菜单「我的跟进」（招生管理）：按归属人汇总我名下联系人的三类互动（跟进记录 / 招生跟进 / 邮件）+ 行内最近摘要 + 就地展开 + 悬停预览 | 已完成 | | P1 | 招生管理 |
| 新菜单「归属人映射」+「我的跟进」按映射管控：筛选改「用户」、三类勾选框、展开全量罗列、招生跟进/邮件弹窗、统一翻页 | 已完成 | | P1 | 招生管理 |
| 招生跟进改用联系人（新建 / 修改从联系人取数） | 已完成 | | P1 | 招生管理 |
| 招生分析报表（8 个分析区块 + 6 个 KPI 下钻） | 已完成 | | P1 | 招生管理 |
| 联系人去重报表（分组 + 证据 + 建议保留 + 导出） | 已完成 | | P1 | 招生管理 |
| 候选人流失分析（按渠道 / 归属人） | 已完成 | | P2 | 招生管理 |
| 邮件自动归档（IMAP + 附件本地化 + 关联学生） | 已完成 | | P0 | 招生管理 |
| 邮件归档按用户隔离：账户「关联用户」+ 行级数据范围 + 「用户/关联」列 + 归属迁移 | 已完成 | | P0 | 权限安全 |
| 邮件归档：关联权限口径修正（招生/班主任不再 403）+ 邮箱筛选列 + 取消关联入口明显化 + 列表列精简 | 已完成 | | P1 | 招生管理 |
| 邮件归档「关联一致化」：联系人 ↔ 学生 双向补全（保存即时 + 手动重算）+ 空关联形态判据收口（idsOf）+ 置信度门槛 ≥85 | 已完成 | | P1 | 招生管理 |
| 新建账号默认选中真实校区 + 校区改必填（堵 ABAC 校区两个反向坑，7 个账号曾看不到学生） | 已完成 | | P0 | 权限安全 |
| 用户表姓名与飞书对齐（宋琼｜Sally / 吴倩｜Erin / 孙旭峰｜Richard） | 已完成 | | P1 | 数据一致性 |
| 部门成员同步漏抓「根部门（公司）」直属成员，刘玉蓉｜Yvonne 查不到 | 已完成 | | P1 | 组织管理 |
| 用户管理页左侧「部门架构」树：点部门右侧账号列表同步筛选（含下级可切、可分享 URL） | 已完成 | | P0 | 权限安全 |
| 学生档案行级数据范围：角色级「数据范围」+ 人级「学生档案范围」+ 9 个学生模块接入 + 学生全景受限 | 已完成 | | P0 | 权限安全 |
| 学生档案列表/导出补齐字段级脱敏（此前只有详情页脱敏） | 已完成 | | P1 | 权限安全 |
| 角色管理「复制角色」：以现有角色为模板新建（继承权限/菜单/密级/数据范围，不继承内置与锁定） | 已完成 | | P0 | 权限安全 |
| 招生跟进「学生姓名」：选完联系人自动带出（联动 patch 挂错列，自 9-19 起从未生效）+ 改为只读 + 存量 9 条回填 | 已完成 | | P1 | 招生管理 |
| 招生跟进 / 校友跟进 / 实践活动「负责人」默认 = 当前登录用户（并修 `RecordMeta.readonly` 导致负责人**根本存不进去** + 修「保存后点修改联系人看着没带出」） | 已完成 | | P0 | 招生管理 |
| 「负责人」口径收口到 `packages/contracts/defaultFollowupOwner`（一处判据，三个模块共用） | 已完成 | | P1 | 招生管理 |
| 招生跟进：加「学生姓名」列 + 「学生」改名「关联学生」；联系人管理加「学生姓名」列（取 `自定义字段.xsxm`）；学生观察加「沟通方式」；修学生列超链接（根因 `linkBackfill`，17 条只有 2 条有关联 id）；笔记统计「按人」漏人；审计日志「操作时间」缺时分秒 | 已完成 | | P1 | 学生闭环 |

### AI 路由

| 标题 | 状态 | 处理人 | 优先级 | 标签 |
|---|---|---|---|---|
| AI 路由六模块（分组 / 上游账号 / 模型路由 / API 密钥 / 用量 / 审计） | 已完成 | | P0 | AI 路由 |
| AI 网关真实调用打通（DeepSeek 实测 + 计费 + 冷却恢复） | 已完成 | | P0 | AI 路由 |
| AI 路由用户管理（方案待定：复用 or 新建） | 待开始 | | P2 | AI 路由 |

### 报表管理

| 标题 | 状态 | 处理人 | 优先级 | 标签 |
|---|---|---|---|---|
| 学生类报表 4 张（结构概览 / 年级流向 / 入学趋势 / 档案完整度） | 已完成 | | P1 | 报表管理 |
| 笔记统计 / 活跃时段报表 | 已完成 | | P2 | 报表管理 |
| 报表下钻（点人数进名单，带筛选） | 已完成 | | P1 | 报表管理 |
| 报表新卡「使用统计」：跨 5 个模块看谁在用、用了多少（三个「数字会对不上」的口径做成判据 + 37 项单测；上线当天探针抓到 3 处已修） | 已完成 | | P1 | 报表管理 |
| 导出页「学生记录」改为按记录类型分别导出（三合一项不再指向合并前的旧空表）+ 逐类型行数核对 | 已完成 | | P1 | 报表管理 |

### 权限安全

| 标题 | 状态 | 处理人 | 优先级 | 标签 |
|---|---|---|---|---|
| 「我的跟进」可见范围收紧：非系统管理员只能看自己（服务端忽略 `user` 参数 + 用户下拉只回本人 + 前端「仅本人」标签） | 已完成 | | P0 | 权限安全 |
| 菜单级权限点补齐 + 存量角色迁移 | 已完成 | | P0 | 权限安全 |
| 菜单入口口径收口：`read` 蕴含 `enter`（修 9 个角色「有权限却看不到菜单」） | 已完成 | | P0 | 权限安全 |
| 菜单 key 与模块资源 key 对齐（4 处别名收口）+ 合并重复的部门管理资源 | 已完成 | | P1 | 权限安全 |
| 「教务」调整冲销：菜单可见但接口 403 | 已完成 | | P1 | 权限安全 |
| 部门同步接口收权限：`POST /departments/sync` 原本任何登录用户可调 → 收口到 `module:departmentManagement:update` | 已完成 | | P0 | 权限安全 |
| 修复菜单 key 与模块资源 key 不一致导致 4 个菜单静默隐藏（回归修复） | 已完成 | | P0 | 权限安全 |
| 修复权限矩阵保存后管理员被降级回历史快照（自愈缺口） | 已完成 | | P0 | 权限安全 |
| 字段密级脱敏（FIELD_LEVELS 自动脱敏） | 已完成 | | P0 | 权限安全 |
| 审计四件套（创建 / 更新人 + 时间，79 张表） | 已完成 | | P1 | 权限安全 |
| 操作日志 / 登录日志 / 系统监控 | 已完成 | | P2 | 权限安全 |
| 身份模拟（系统管理员二次密码解锁后，可以任意账号身份浏览 ACMS，进入/退出全程留痕） | 已完成 | | P1 | 权限安全 |
| 会议纪要可见范围（公开 / 部门内可见 / 指定部门可见 / 指定用户可见 / 仅自己可见 + 部门负责人跨部门可见） | 已完成 | | P0 | 权限安全 |
| 知识库配置「归属人」= 该配置服务的账号主人（管理员代建时不再归到创建者名下）+ 存量归属数据更正 | 已完成 | | P0 | 权限安全 |
| 修「已被关联到知识库配置，却仍被要求自己配凭证」：两套可见性判据不一致（`configured` 与 `viaSource`） | 已完成 | | P1 | 权限安全 |
| 学生记录「权限矩阵里勾了却不生效」：补声明 `module:weilingContacts:*` + 容器行 `typeScope.allTypesModuleKey` + 403 提示改人话 | 已完成 | | P0 | 权限安全 |

### 基础设施

| 标题 | 状态 | 处理人 | 优先级 | 标签 |
|---|---|---|---|---|
| 飞书 → PostgreSQL 数据层迁移（79 表全量切读） | 已完成 | | P0 | 基础设施 |
| Blue-Green 零停机部署 | 已完成 | | P0 | 基础设施 |
| 云盘 / 云文档 / 字典内化 | 已完成 | | P1 | 基础设施 |
| 部门管理（部门树 + 部门成员） | 已完成 | | P2 | 基础设施 |
| 外部客户端接入（CLI / MCP / API 令牌，让 agent 调用 ACMS） | 已完成 | | P1 | 基础设施 |
| 全站按钮权限收口（CrudPage `moduleKey` 全覆盖 + 只对已声明动作生效） | 已完成 | | P0 | 权限安全 |
| 知识库配置按用户隔离（「关联用户」多选 + 可见性判据唯一化） | 已完成 | | P0 | 权限安全 |
| 笔记正文落库（`noteBody`：智能总结与原始记录分列） | 已完成 | | P1 | 基础设施 |
| 「重新收取」笔记正文（异步 + 进度 + 幂等 upsert） | 已完成 | | P1 | 基础设施 |
| 角色管理页重构（菜单可见性并入矩阵 + 列表视图按模块罗列） | 已完成 | | P0 | 权限安全 |
| 权限判据双轨制收口（12 处自建 controller 切到 `module:` 体系 + 菜单白名单修复） | 已完成 | | P0 | 权限安全 |
| 笔记音频：来源字段落库（根因）+ 每日 06:30 自动抓取 + 列表标出「待抓取」 | 已完成 | | P1 | 基础设施 |
| 全站下拉筛选统一：`FilterSelect` 抽成公共组件（补多选与 `clearable`）+ 清 3 份副本 + 28 处原生 select 收口 | 已完成 | | P2 | 基础设施 |
| 学生详情页「关联笔记」聚合：全路径（本人 + 各类学生记录 + 招生跟进）+ 来源标注 + 跨模块权限过滤 | 已完成 | | P1 | 学生闭环 |
| 学生档案「相关邮件」：修掉关联字段等值筛导致恒 0 条（邮件已关联却看不到）+ 补齐发件人 / 附件清单（文件名可点下载）/ 发送时间到日 | 已完成 | | P1 | 学生闭环 |
| 「我的笔记」归档 / 激活（自建状态表）+ 状态筛选（默认只看「有效」）+ 归档笔记不可转换（409 `NOTE_ARCHIVED`） | 已完成 | | P1 | 基础设施 |
| 「我的笔记」筛选收口：状态下拉去掉重复的「全部」+ 触发器统一成「标签：当前值」+ 提示条 N 的口径改为「挡掉 ∧ 归档」 | 已完成 | | P1 | 基础设施 |
| 新菜单「会议室助手」（组织管理）：读飞书会议室**房间 / 层级 / 设备**，看可用度、找空闲（含真实接口结构三处修正：`room_level_id`、「楼栋」取最细一级、`freebusy` 返回对象非数组） | 已完成 | | P1 | 组织管理 |
| 「我的笔记」每日自动归档到飞书云盘：01:00 有效∧标题含 IDP → `VULJ…`；01:30 全部有效 → `K6Ij…`（按人分文件夹 + 明细/总结两份 md + 日期前缀 + 已复制过跳过 + 失败告警） | 已完成 | | P1 | 基础设施 |
| 新菜单「定时任务」（后台管理·仅系统管理员）：归档任务从代码常量搬成数据行 ⇒ 可**增删改** + 操作列「运行」「补归档」；支持每天/每周几、启用开关、输出内容（明细/总结）、按人分文件夹、补跑窗口；改目标文件夹时提示是否补归档 | 已完成 | | P1 | 基础设施 |
| 「定时任务」升级为通用调度器：任务类型（笔记归档 / 卫瓴联系人同步 / 邮件收取）+ 频率（每天 / 每小时 / 每15分钟）；卫瓴默认每天 07:00、邮件收取每15分钟；删除三处硬编码定时器；每分钟 tick + 槽位去重 | 已完成 | | P0 | 基础设施 |
| 学生档案：字段「入学年份」改名**入学年月**（四层同批：表单/字典/`acms_fields`/82 行数据键）+ 新增「入学年份」（2021–2030）与「Arete入学年」（第1年–第10年）+ 列表隐藏 4 列、新增 2 列 | 已完成 | | P1 | 学生闭环 |

### 数据质量

| 标题 | 状态 | 处理人 | 优先级 | 标签 |
|---|---|---|---|---|
| 首页 logo 旧附件待人工重传 | 待开始 | | P2 | 数据质量 |

## 三、待办与阻塞（6 项）

> 每项都有对应的 GitHub Issue（WorkBuddy「项目 → 计划」读的就是 Issues，两处保持一一对应）。
> 状态变更时**两处一起改**：这里改表格、issue 改 state。

| 优先级 | 标题 | Issue | 依赖 / 说明 |
|---|---|---|---|
| **P0** | 教学班数据补录 | [#1](https://github.com/sunxufeng/acms/issues/1) | **阻塞其他项**：教学班表为空，课程规划的「部署环节到课次」无法验收 |
| **P2** | AI 路由用户管理 | [#3](https://github.com/sunxufeng/acms/issues/3) | 需先定方案（复用 vs 新建），再排期 |
| **P2** | 首页 logo 重传 | [#4](https://github.com/sunxufeng/acms/issues/4) | 纯人工操作 |
| **P2** | 学生详情页「关联笔记」的可见性口径 | [#12](https://github.com/sunxufeng/acms/issues/12) | 面板入口沿用 `module:getnote:read`（**与改动前一致**）⇒ 实测 28 个角色均未显式持有、普通教师 403。待定：A 保持现状 / B 按「能看这个学生」放行 / C 分层给（跨模块泄漏已由来源模块权限挡住） |
| **P2** | 会议纪要加「关联学生」字段 | [#13](https://github.com/sunxufeng/acms/issues/13) | 该表**没有学生字段** ⇒ 6 条会议纪要关联的笔记无法归属到学生（本次聚合有意跳过）。需加关联字段 + 声明 `studentMatch` + 登记 `NOTE_ENTITY_TYPE_TO_PATH`；存量 6 条人工补 |
| **P2** | 学生档案两处「字典选项与实际数据不匹配」 | [#14](https://github.com/sunxufeng/acms/issues/14) | ① `Arete毕业届`：字典「第1届…第6届」vs 数据「第六届（2026年入学）」⇒ 下拉选不中已有值；② `入学年级`：字典 `G8–G12` vs 数据 `Pre-1/Pre-2/…` ⇒ **该筛选框筛任何值都返回 0**。**先要定「以字典为准还是以数据为准」**，改错方向会把真实数据变成"选不中" |

> ✅ 2026-09-22 已关闭 [#2 家长 / 学生门户](https://github.com/sunxufeng/acms/issues/2)：`1674d47` + `a469a0f`。
> 此前**文档标记完成、issue 仍开着**（两处不一致），本次补齐。

### 上线后待人工（**不属于计划条目**，不计入 54 项统计）

> 这些是「代码已就绪、但需要人在界面上点几下才生效」的初始化动作。
> 不记录在第二节总表里，避免污染计划条目数；做完可逐条划掉。

| 事项 | 在哪做 / 说明 |
|---|---|
| 配「考核类型」 | 教学管理 › 考核类型 —— 名称 / 颜色 / 缺省权重 / 是否计入总评。成绩册列头色块与第二层权重缺省都取这里 |
| 建「成绩批次」 | 教学管理 › **成绩批次**（2026-09-20 起有页面）—— 学年 + 学期 + 日期区间 + 舍入 / 免考 / 缺考口径。**没有批次就没有期末总评** |
| 给成绩等级配「绩点」 | 教学管理 › **成绩等级体系**（2026-09-20 起有页面：左栏体系、右栏等级）—— 不配绩点则 GPA 与班级排名为空（页面会明说「未配置绩点」，不会显示 0.00） |
| 给存量角色补授权 | 角色管理 › 权限矩阵 —— 矩阵存的是**已派生的完整权限集**。⚠️ 2026-09-20 新增的四个模块（成绩批次 / 成绩等级体系 / 成绩类型权重 / 学生成绩目标）**已通过 v4 迁移从旧权限点自动继承**（零行为变化），若要收窄再手工取消勾选 |
| 配 `IMPERSONATE_PASSWORD`（可选） | 生产环境变量 —— 配了则以它为准，代码内默认值失效。不配也能用，但默认值写在源码里，只防误操作不防攻击。**同一个密码管「身份模拟」与「令牌管理」两个闸**（实测生产当前 `passwordSource=default`） |
| 签发第一条真实令牌 | 后台管理 › 令牌管理 —— 系统管理员 + 二次密码。建议**按用途分开签**（只读给查询、可写给自动化），不用了立刻吊销 |
| 修正卫瓴报名表 `xsxm` 脏值 | **卫瓴侧（上游）** —— 本批 2 条招生跟进的「学生姓名」带出的是手机号 `13482427338`（家长把「学生姓名」栏填成了自己的号）。字段已按需求改为只读，**系统内改不了**，只能在卫瓴报名表改 |

> ✅ 2026-09-14 已消解：出勤率报表（含考勤终态审核）、作业与成绩册联动 —— 见下方里程碑。
> ⚠️ **考勤数据尚未积累**：生产「学生考勤记录表」当前 0 行，出勤率报表数字全为 0 属预期；
> 有打卡/补录数据后自动出图。另：考勤码表为空时按内置默认 7 码计算。

---

## 四、已完成里程碑

### 2026-09-22 深夜 新菜单「定时任务」：归档任务可增删改 + 手动运行（`53a3445` + 权限修复 `f5c5602`）

- 任务从代码常量搬成数据行（新表 `noteArchiveJob`），页面走通用 CRUD；
  种子两条**沿用 `idp` / `all` 作为行 id**（它同时是归档记录的外键）⇒ 昨晚已归档的
  1412 个文件不会因为"任务上云"被重传一遍。
- 口径全在 contracts：文件夹链接自动取 token、时间/周几/输出内容容错解析、
  `shouldRunArchiveJob` 补 `enabled` 与 `weekday` 两个维度。
- 权限：`adminOnly` + legacy 依据 `admin:user`（**只有系统管理员持有**）、
  **不抬版本**。🔴 踩到并修掉：legacyRead/Write 写 `null` 会让**管理员自己也没权限**
  （模块权限是按 legacy 派生的，派生不出来 `healLockedRoles` 也救不回）——
  已加 4 条单测钉住（含"十个其它角色一个权限点都没有"）。
- 验收（七项全绿）：任务表两条种子（id 正确）· 列表/新建（**粘链接**可存）· 修改 ·
  删除无残留 · 教师本人 403（列表与体检两处）· 菜单自愈出现 `scheduledTasks` ·
  **运行真跑**：`候选 1，跳过 28，上传 0`（删掉 1 条记录制造待归档 → 重跑补齐，
  云盘同名被跳过 ⇒ 没产生重复副本）· 「上次运行」两列回写 ·
  线上页面 `/scheduled-tasks` 200 + 产物含全部关键词。
- 验证：api/web typecheck · **vitest 498 passed / 27 文件** · i18n lint 539 ·
  BUILD_ID `KyKAM1JXGe-WRDbV4qHZ-`（槽 3002/3102）。

### 2026-09-22 晚 「我的笔记」每日自动归档到飞书云盘（`efe32d5` + 修复 `099ad93`）

峰哥要求：服务器上定时任务，每天 **01:00** 把「有效 ∧ 标题含 IDP」的笔记、**01:30** 把
「全部有效」的笔记复制到两个飞书云盘文件夹；按人建子文件夹、每篇出**明细**与**总结**两份 md、
文件名前缀日期、已复制过跳过。实现 `apps/api/src/note-archive/`（口径在 `contracts/note-archive.ts`）。

- 🔴 **数据源实测三选一**：笔记快照表 **780（有效 763）** > 正文表 658 > 上游实时 767。
  取快照表（超集，含标题/归属人/创建时间/总结），**明细**另从正文表取；
  122 篇只有总结没有明细 ⇒ 只出总结并计 `noDetail`，不静默跳过整篇。
- 🔴 **幂等判据 = 新增的笔记归档记录表**（行 id `<笔记ID>__<任务>`，独立表），**不是**
  「云盘里有没有同名文件」：标题会截断、同人同日同标题真实存在（`2026-09-12 无内容` 同人 2 条）。
  ⇒ 文件名保留笔记 ID：`YYYY-MM-DD 标题-明细__<id>.md`。
- 🔴 **文件夹名归一**：库里 `刘佳音 | Joy`（半角竖线+空格）vs 云盘 `刘佳音｜Joy`（全角）——
  不归一会给同一个人建出两个近似重名文件夹。空归属人 → 「未归属」。
- 🔴 **定时器加「补跑窗口」**：只判「已过计划时刻 且 今天没跑过」的话，**任何一次部署重启**
  都会被判成「今天该跑却还没跑」⇒ 当场跑全量。**上线当天实测踩到**（21:13 部署把两个任务
  同时触发了）⇒ 改成 `今天没跑过 && 已到点 && 现在 ≤ 到点 + 6h`，抽成纯函数 + 单测两个方向。
- **写入用用户身份**：云盘在个人空间，`tenant_access_token` 访问不了 ⇒ 用峰哥的用户令牌
  （`getUserAccessToken` 自动 refresh，实测生产 18 个用户都有持久化令牌）。
- **失败必告警**：令牌撤销/上传失败发飞书 IM（静默不归档是最坏的失败方式）。
- **接口**（仅管理员）：`GET /note-archive/{jobs,status,check}` + `POST /note-archive/run {job,limit}`。
- **顺手修 bug**：`listDriveFilesAll` 翻页没传 `pageToken`（每次拉第一页 ⇒ >100 条的文件夹拿不全）。
- **验证**：`check` 全绿（令牌可用 / 两文件夹可达 / 待归档 IDP 29 · 全量 763）→
  实跑 IDP 全部 29 篇 + 全量 763 篇，**失败 0**，产物命名与文件夹与预期一致；
  api/web typecheck · **vitest 469 passed / 25 文件**（新增 25 条，含「同人同日同标题不撞名」
  「归一幂等」「窗口判据」；测试还抓出一个空日期生成前导空格文件名的真 bug）。
  ⚠️ 部署 BUILD_ID `V93rUJXKcSErfiQpYF2Q6`（槽 3002/3102）。
- ⚠️ **交付时已提醒峰哥**：① 763 篇原文（含学生姓名/家校沟通细节，**未脱敏**）进云盘 ⇒
  可见范围由云盘权限决定；② 失败会发飞书告警。

### 2026-09-22 傍晚 学生档案：选完「入学年月」自动带出「入学年份」+「Arete入学年」（`f2220f1`）

峰哥要求：选完入学年月自动填上那两个字段，**带出后仍可手动改**。

- **规则**（放 `packages/contracts/src/student-enroll.ts`，前后端共用一份）：
  `入学年份 = 20 + NN`；`Arete入学年 = 第(入学年份 − 2020)年`
- 🔴 **两条读法都能解释范例，必须用全量数据排除一个**：另一读法「入学后第 N 年
  （当前年份 − 入学年份）」在范例（23秋季→第3年）上同样成立，但套全量会把 `26秋季`（**61 人**）
  算成 **第 0 年**（不在字典第1年–第10年）⇒ 被否。
  **只拿一个范例反推规则时必须做这一步**，否则整批数据按错规则填、且每个值都在字典范围内，
  肉眼看不出来。采用的规则与既有「Arete毕业届：第四届 = 2024 年入学」自洽。
- **三条边界**：① 推导不出就**不动**（清空「入学年月」不会抹掉那两个字段 —— 便利填充不该变成
  静默的数据删除，与「招生跟进选联系人带出学生姓名：没填就不覆盖」同源）；
  ② 超出字典范围返空（`2031 → 第11年` 不写 —— 写字典里没有的值比不填更难排查）；
  ③ 是**便利填充不是不变式**：只在「入学年月」变化时带出，服务端**不做**强制覆盖。
- **实现**：`contracts` 纯函数（`enrollYearFromMonth` / `areteEnrollYearLabel` /
  `deriveEnrollFields` + 字段名常量）；`StudentForm` 的 `FieldDef` 加 `hintKey`（存 students
  命名空间 key，走 `ts()` 而非 `tl()`）、`入学年月` 的 select onChange 调派生、控件下方加
  `.form-hint` 小字说明（含「例：23秋季 → 2023 / 第3年」）。
  字段名一律用 contracts 常量比较，**不许手写中文字面量**（手写字符串改名后必然脱钩且静默失效）。
- **验证四层**：① 单测 12 条（`student-enroll.test.ts`），其中一条专锁
  「26秋季必须是第6年、绝不能是第0年」—— 这是两个读法的分界，删掉等于允许整批填错；
  ② **规则 ↔ 线上 82 行数据：不一致 0**（用**部署到服务器上那份** contracts 跑的，不是本地）；
  ③ 源码级**接线守卫** 5 条（`student-enroll-wiring.test.ts`，读 `StudentForm.tsx` 断言分支用常量、
  调用了派生、算了就写回、提示还挂着）；④ 本地探针页真渲染：提示文案 +
  字段顺序（入学年月 → 入学年份 → Arete入学年 → Arete毕业届）+ 预填回显。
  全量 **vitest 446 passed / 24 文件**。
- ⚠️ **未覆盖**：下拉「点一下」的真实交互没有自动化环境（仓库里没有浏览器测试），
  接线靠源码守卫 + 纯函数单测兜，**需要人工点一次确认**。
- 交付链：推 `f2220f1` → 打包 `TAR_BUILD_ID` 与本地一致 → 部署 BUILD_ID
  `FEFFnTgbZx7EoTd6kmI2T`（槽 3002/3102）。⚠️ 第一次打包在**后台**跑被沙箱拦
  （`cp … pdfkit/js/pdfkit.js: Operation not permitted`）⇒ tar 没重建；包装脚本的
  BUILD_ID 一致性校验把这一版**拦住了、生产未动**，改前台重跑后通过。

### 2026-09-22 下午（2）学生档案字段改名与新增：`入学年份` → `入学年月`（四层同批，`3044f5a`/`d9143e4`）

峰哥：「入学年份字段描述改为入学年月，字典表也从入学年份改为入学年月；新增一个字段入学年份
（2021–2030）放在入学年月后面；新增 Arete入学年（第1年–第10年）放在 Arete毕业届前面；
列表隐藏 arete毕业届/来源渠道/生源跟进/更新时间，当前年级列后加 Arete入学年、入学年月」。

- **改名理由**：原值 `26秋季 / 26春季` 是**学年学期**不是年份 —— 名字与值不符。
  「入学年份」这个名字腾给新增的纯年份字段，两者并存。
- 🔴 **四层同批**（少改一层 = 静默丢数据，值还在但没人读得到）：
  ① 表单 `StudentForm.tsx`（字段顺序的唯一来源）② 字典（种子 + 生产文件）
  ③ `acms_fields.name`（**沿用原 `field_id`、保留 15 个选项**）④ 记录表 `data` jsonb 键名（**82 行**）
- 🔴 **连带引用 7 处，全是静默失效**（都不报错）：`student.dto.ts` 三处筛选参数 ·
  `student.service.ts` 筛选 + 内存等值清单 · `reports.service.ts` `DIMENSION_FIELDS`/`COMPLETENESS_FIELDS`
  · `panels.tsx` 入学趋势维度 + `drill({字段})` · `reports/page.tsx` `FILTER_KEYS` ·
  `dict.service.ts` 建字段清单 · i18n（labels 15 条 + students 4 条）。
  **报表维度/筛选键指向不存在的字段时，分组统计整维度为空、筛选恒 0，零报错。**
- **新增字段**：`入学年份`（2021–2030）· `Arete入学年`（第1年–第10年），各插一行 `type=3` + 选项；
  存量 82 名学生的「Arete入学年」**先留空**（「第N年」口径未定，推算会造出一批像真的错数据）。
- **列表**：隐藏 4 列（`Arete毕业届 / 来源渠道 / 生源跟进状态 / 更新时间` —— 后三列生产实测
  **全 0 行有值**）；在「当前/入学年级」后插 `Arete入学年`、`入学年月`。
  🔴 表头 `COLS` 与手写 `<tbody>` 是**两份**，多一列少一列不报错、只整行串位 ⇒ 本次核 7 = 7。
  本页无导出按钮 ⇒ 隐藏列不影响导出（`/export` 按表读 `acms_fields`）。
- **筛选框**：`入学年份`→`入学年月`，并新增两个（触发器 7 → 9）。
- **验证**：① 逐行比对改前/改后值 **不一致 0**（82 行值分布与改前完全一致）
  ② 接口 `入学年月=26秋季 → 41` / `26春季 → 20` / 两个新字段 → 0
  ③ 产物核 `COLS` 顺序 + 旧列键归零 + `STUDENT_SECTIONS` 顺序断言 3/3
  ④ typecheck · i18n lint 539 条 · BUILD_ID `dokkNnk2JJSLfwDJLfOWJ`（槽 3002/3102）
- **登记** [#14](https://github.com/sunxufeng/acms/issues/14)：`Arete毕业届`（字典「第1届…第6届」
  vs 数据「第六届（2026年入学）」）与 `入学年级`（字典 `G8–G12` vs 数据 `Pre-1/Pre-2/…`
  ⇒ **该筛选框筛任何值都返回 0**）两处字典漂移，按峰哥决定**先只报告**。
- **存量回填**（同日随后，峰哥给范例）：按「入学年月」把两个新字段补齐 —— 范例是
  `沈嘉铖 STU-00266 · 23秋季 → 入学年份 2023 / Arete入学年 第3年`。
  反推规则：**入学年份 = 20 + `NN`**；**Arete入学年 = 第(入学年份 − 2020)年**
  （2021→第1年，与「Arete毕业届：第四届 = 2024 入学」同一套编号；另一读法「入学后第 N 年」
  被数据否掉 —— 26秋季会算成第 0 年）。
  82 行全部有 `入学年月`、其中 **81 行待填**（第 1 行已是范例，等于规则自校验通过）；
  回填后 **82/82 两个字段非空、与「入学年月」不一致 0**，分布：2023×1 / 2024×8 / 2025×12 / 2026×61。
  接口复核 `入学年份=2026 → 61`、`Arete入学年=第6年 → 61`、`第3年 → 1`、`23秋季 → 1`。
  快照 `/opt/acms/data/enroll_year_fill_before_20260922-165719.json`（**改前那份，回滚用**；
  同目录 `-165729` 那份是重试后的"改后"状态，别拿它回滚）。
  ⚠️ **遗留**：新建/修改学生时这两个字段**不会自动推导**（需手填或重选「入学年月」）——
  若要自动化，做法与「招生跟进·选联系人带出学生姓名」同款：把联动 patch 挂在**触发变化的列**
  （入学年月）上。

### 2026-09-22 下午 学生详情页「关联笔记」聚合 + 列表页收两个筛选框（`4443cb0`/`bfa24eb`）

峰哥：`/students` 隐藏「招生来源 / 生源跟进」筛选框；点学生姓名进详情页后，**关联笔记要能看到
与该生相关的全部笔记**（他的各类学生记录都能最终连到笔记）。先设计 → 确认 → 开发。

- 🔴 **原面板为什么是空的**：`NotePanel` 只查「实体类型 = 学生档案」，生产实测这种关联 **0 条**；
  真实笔记全挂在**学生记录**（19 条）与**招生跟进**（10 条）上。
  顺带查出**口径不一致**：那 19 条的「实体类型」写的是「学生记录」，而记录详情页按
  「IDP沟通 / 家校沟通 / 日常跟进…」查 ⇒ **这些笔记在记录详情页里也看不到**（写入方在现有
  代码里没找到，疑似早期版本或一次性脚本）。⇒ 取数改为「拿实体 ID 反查归属表」，
  **不按实体类型白名单**，历史写歪的类型名照样捞得回来。
- **实现**：`contracts/student-note-sources.ts`（`SECTION_LABELS` 从 student-360 **提到 contracts**
  避免漂移出第二份 + `NOTE_ENTITY_TYPE_TO_PATH` 归一历史别名 + 前后端共用返回类型）；
  `GET /getnote/links/by-student/:studentId`；取数「先读小表（35 行）→ 逐条 `base.get` 定点取 →
  按 `meta.studentMatch` 判归属 → 按来源模块权限过滤」，**不拉全表**（比 student-360 的全表扫描快得多）。
- 🔴 **权限**：没有来源模块权限的**整块跳过**并记入 `hiddenSources`（前端明示「有来源你无权限查看」）
  —— 不跳就是泄漏别的模块的笔记，不记则用户以为这个学生真只有几篇；三合一记录另过**类型域**权限。
- **前端**：`NotePanel` 加可选 `studentId`（**同一组件的模式开关**，不新开组件：候选搜索、凭证校验、
  错误处理、覆盖式回写只有一份）；聚合视图含来源徽标 + 来源 chips（带计数）+ 每篇下列出
  「是哪条记录带出来的」（可点进记录）+ 按姓名匹配的招生跟进打标；**「解除」只给直接关联的条目**
  （一篇笔记可被多条记录关联，聚合视图里解除说不清解的是哪条）。`/getnote` 加 `?note=` 深链。
- `/students` **删除**「来源渠道」「生源跟进状态」两个筛选框（含初始 state；不是 display:none ——
  否则留着两个恒空参数，接手的人会以为"有筛选却筛不动"）。列表列与 CSV 导出不动。
- **验证**（真实数据）：夏子墨 **3 篇**（IDP沟通 / 家校沟通 / 日常跟进各 1，含来源记录标题与日期）
  ✅ 断言全过；空学生返回 `notes:0` 不报错 ✅；不存在的 id 返回 **404**（不是 500）✅；
  列表页触发器 **9 → 7** ✅；`links/by-student` 已进前端 chunk ✅。
- ⚠️ **待峰哥定的一处口径**：面板权限沿用 `module:getnote:read`（**与改动前完全一致**）——
  实测 28 个角色里**没有角色显式持有**该权限，管理员类角色能进（院级管理实测 200、教师本人 403）。
  普通教师打开学生详情页仍会看到权限报错，这是**改动前就有的状态**，不是本次引入。
  要不要放开（例如「能看这个学生就能看他的笔记」）属独立决策。
- 另：`/getnote` 深链用 `window.location.search` 而不用 `useSearchParams()`，避免给该客户端页
  平添 Suspense 边界；一次 `useRef` 守卫防止 openDetail 身份变化时把用户关掉的详情又弹回来。

### 2026-09-22 中午（3）统一下拉筛选框 · 第二批：原生 select 收口（`ba4a672`/`4853f56`）

峰哥「按你的建议执行」= 清单第 1–11 项换、保留项不动、批次/班级类先量选项条数。

- **先剔掉一半**：58 处原生 select 里约 30 处**不是筛选框**（`StudentForm`/`UserForm`/
  `ColumnEditor`/`PlanForm`/`AgentForm`/`ApiConfigForm`/`api-tokens` 表单/`notifications` 选模板…）
  —— 是「录入值」不是「筛选条件」，不动。
- **实测选项条数**（承诺 >30 才保留原生）：班级 **6** · 学生当前年级 6 · 入学年份 6 ·
  成绩批次 **1** · 卫瓴归属人 8 ⇒ **无一超 30，全部可换**。
- 🔴 **给组件补了一个能力 `clearable`**：剩下那些里混着一类语义不同的**必选参数**
  （成绩册批次/班级、导出业务表）：值恒非空、页面逻辑依赖它 ⇒ 给「全部」项不但没意义，
  点下去还会把状态清成空字符串。`dedup` 的「置信等级」更微妙 —— 三个值里有一个**恰好叫
  「全部（含仅同名）」**，再加内建「全部」就是两个"全部"（踩到铁律 1）。
  ⇒ `clearable?: boolean`（默认 true，第一批行为不变）：false 时不渲染「全部」项、空值显示 `—`。
- **覆盖 10 文件 / 28 处**：`reports/page.tsx`（`FILTER_KEYS` 通用循环，改一处该组报表全统一）、
  `reports/weiling`(4)、`reports/dedup`(3)、`reports/attendance`(2)、`reports/exam`(5，**两套布局都改**)、
  `exam-grades`(3)、`markbook`(5)、`export`(1)、`impersonate-logs`(1)、`meeting-rooms`(1)。
- **按评估保留原生**：`markbook-weights` 选班级/选考核组 —— 那是「请选择 XX」的**向导式选择**
  （空 = 还没选），触发器写「班级：全部」会误导成"所有班级的权重"。
- 顺手补两处信息丢失：`exam-grades`/`markbook` 的「暂无批次/暂无班级」原先只是下拉里的一个 option，
  换组件后必丢 ⇒ 改成空列表时旁边一行文字提示。i18n 补 3 条（`labels.批次`、`meetingRooms.capacityAny/Min`
  —— 原文案自带「容量：」前缀，因为原生控件没有标签位）。
- **验证（三套）**：① 本地 SSR 探针页 6 种状态全对（可选空`：全部`/可选有值/必选有值/
  必选空值`：—`/必选多值`：a、b`/数字值`0→不限`）；② **线上真页面** SSR ——
  `/export`「业务表：学生档案（studentProfile）」、`/impersonate-logs`「动作：全部」、
  `/meeting-rooms`「需要容纳人数：不限」；③ 新旧产物对比 —— 各页面 chunk 里统一组件
  **0 → 1**，手写文案「全部班级/全部科目/归属人：全部/客户阶段：全部」**旧 1 个文件 → 新 0**。
- ⚠️ **产物断言的一个判据陷阱（本次踩到）**：不能用「页面 chunk 里 `filter-select-trigger`
  出现次数」当"这页有几个筛选框" —— 组件被打包成独立/内联的一份，页面里用 5 个也只出现 1 次。
  更可靠的判据是**新旧对比**（0 → 1）与**手写文案消失**。
- typecheck · i18n lint 539 条 · BUILD_ID `e0EExsDzdbReVRClSc2U`（槽 3002/3102）。

### 2026-09-22 中午（2）统一下拉筛选框 · 第一批：抽出组件、清掉 3 份副本（`3fba394`/`da0322e`）

峰哥报 `/students` 的筛选框与全站长相不一致，要求先出统计 + 两种方式优缺点；确认后按建议
**第一批开工、第二批逐项单独评估**。

- **根因**：`FilterSelect` 只存在于 `CrudPage` **内部**、没有导出 ⇒ 自建页面要筛只能
  ① 复制一份、② 改用原生 `<select>`。于是漂移出 4 种长相（全站盘点）：
  `/students`（只显示标签，看不出当前筛了什么，但多了多选）、`/ai/skills` 与 `/ai/config`
  （两份逐字相同，空值时**少了「：全部」**）、报表/成绩册等 11 处原生 select、`/student-360` 表单标签式。
- **本批**：① 组件抽成 `components/FilterSelect.tsx` **并导出**，多选并进来（`multiple`）——
  这是 `/students` 当初留副本的唯一理由；② 显示规则固化「标签：当前值」/ 空值「标签：全部」，
  「全部」项两种模式都在首位 ⇒ 下拉结构一致，不再需要单独的「清除筛选」行；
  ③ 三处副本删除改用统一组件（净减 32 行）。
- **两处有意取舍**：单选**重复点已选项不取消**（与既有 50 个模块页手感一致，改 toggle-off 会动到全部页面）；
  多选值顿号连接（英文逗号）后交给触发器 CSS 截断，并加 `title` 供 hover 看全。
  类型用联合类型而非 `string|string[]` ⇒ 单选调用方签名无需改动。
- **验证（三套证据，都真跑了）**：
  ① **本地 SSR 页面 HTML**：`/students` 9 个触发器全部渲染成「标签：全部」（`title` 同步）；
  ② **临时探针页真渲染 5 种状态**（验完即删、未入库）：单选空 `：全部` / 单选有值 `：在校在读` /
  多选空 `：全部` / 多选三值 `：在校在读、已录未报到、毕业` / `optionLabels` 生效 `：刘攀扬｜Amy`；
  ③ **线上新旧产物计数差**：`filter-check`（多选分支）在 ai/skills、ai/config 的 chunk 里
  **0 → 1**，students 的「：」**2 → 4**；chunk 内可见统一组件的 `title:"".concat(t,"：").concat(...)`。
- 另：web typecheck · i18n lint 544 条 · 副本残留 0 · BUILD_ID `127qacGeDmiDobrZV13Gg`（槽 3001/3101）。
- 📌 **第二批待逐项评估**（58 处原生 select，需按「筛选 vs 表单字段控件」分别判断，已出清单待峰哥逐项定）。

### 2026-09-22 中午 知识库配置「归属人」语义修正：代建时归属账号主人（`010affb`）

峰哥指出：`Michael Get Note` 是孙旭峰（管理员）**替赵光宇｜Michael** 建的配置 —— 即
**「归属人」= 这条配置服务的账号主人，不是创建者**。而 `sources.service.create()` 原先写死
「新建的配置永远归属创建者」（注释还写着「管理员也不能替别人建」）⇒ 那条配置的归属人写成
孙旭峰，派生出来的**笔记归属、按归属人筛选与统计全部归错人**（该配置名下 3 条笔记实测如此）。

- **代码**：`create()` 改三级判据 —— ① 显式传 `归属人ID` ⇒ 尊重调用方；
  ② 否则「关联用户」**恰好一人** ⇒ 用那一位（代建时被代建者就在这里）；③ 都没有 ⇒ 创建者自己
  （单人自建行为**完全不变**）。关联用户存 record id、归属人存 openId ⇒ 新增 `userByRecordId()`
  反查，查不到回落创建者并留痕，不阻断建配置。前端「关联用户」hint 同步改写。
- **数据**（已修，快照 `/opt/acms/data/getnote_owner_fix_before_*.json`）：
  配置表 `Michael Get Note` 归属人 → 赵光宇｜Michael；`Richard Sun Get Note` 的「孙旭峰」
  统一为「孙旭峰｜Richard」（用户表正式姓名，否则同一人在「按归属人」下分叉成两个）；
  正文表 **27 行**（Michael 3 + Richard Sun 24）同步更正 —— 只改归属人两键、幂等。
- **验收**：① 代建场景（管理员建配置 + 关联用户只有 Michael）⇒ 归属人 = **赵光宇｜Michael** ✅；
  ② 自建场景（不传关联用户）⇒ 归属人 = 创建者 ✅；③ 测试配置 0 残留 ✅；
  ④ 端到端：以**赵光宇｜Michael 本人**身份打开「我的笔记」⇒ HTTP 200、9 条笔记归属都显示
  「赵光宇｜Michael」（含被修正的 3 条）✅。BUILD_ID `-MRfaXEXqFhTNCEB0lGSD`（槽 3002/3102）。

### 2026-09-22 上午 笔记音频：修根因（来源字段）+ 每日自动抓取 + 待抓可见（`0aa13a2`）

峰哥报「`/getnote` 还是有记录音频没法播放」。全量体检后：**文件没丢**（564 条音频文件缺失 0、
接口正常），真缺口是 **8 条「有录音但从未被抓」的笔记**（列表里连播放按钮都没有）。
按建议做了三件（B 跳过）——**A 修因、C 防复发、D 让缺口看得见**：

- **A 根因**：`fetchNoteDetail` 只在 `owner` 非空时写 `_sourceName/_sourceRecordId/_owner/_ownerOpenId`，
  而 `owner` 只有**管理员聚合路径**才带 ⇒ 普通用户看过的笔记落库后来源配置恒为空 ⇒
  管理员代抓时 `pickSourceEntry` 匹配不上 ⇒ 回落到自己的凭证 ⇒ 上游一律 `10008 权限不足`。
  → `visibleCredForNote` 改造成 `visibleEntryForNote`（返回**整条配置**而不是只回 cred），
  `detail()` 非管理员分支用它构造 owner。
  ⚠️ 不改 `adminCredForNote`（那就是「猜凭证」的 B 方案，已明确不做）。
- **C 防复发**：原先音频抓取**只有界面上那个按钮** —— 09-18 抓完一轮后连着 4 天没人点，
  于是 09-19 之后进来的 8 条一直没音频。→ 每日 **06:30（北京时间）** 自动抓；
  与手动入口共用 `runRefetchAudio`，唯一差别是没有触发者 ⇒ 选不到来源配置的**跳过并留痕**
  （`job.noCred` + 日志），不拿别人的凭证猜。
  🔴 **不用 `setInterval(24h)`**（项目里 `weiling.service` 是那么写的）：蓝绿部署每次
  **重启进程**都会把计时清零，部署一勤就永远等不到那一刻 —— 而 09-18 之后正好天天在部署。
  改成「每小时醒一次，按**日期 + 时刻**判断今天跑没跑」，与进程活了多久无关；
  重启最坏多跑一次，任务本身按音频状态幂等。时区显式用 `Asia/Shanghai`（不依赖服务器时区）。
- **D 缺口可见**：新增 `noteAudioFlags`（**一次**主键查询同时给「元信息」与「待抓」），
  列表/详情返回 `_audioPending`，前端显示「⏳ 待抓取」。
  原先前端只看 `note_type === 'recorder_audio'`，而报障那批**类型并不是它** ⇒
  有录音却没抓的行在列表里什么都不显示，跟纯文本笔记长得一样。
- **存量**：8 条缺口已全部补齐（`stored=7 failed=0` + 1 条首轮成功），
  终态「有附件但无音频」= **0**，已保存 564 → **572**。
- **顺带清历史欠账**：正文表原有 **54 行**「来源配置 / 归属人」为空的老数据
  （已有音频、不影响播放，但让「来源 / 配置名称 / 归属人」三个筛选对它们失效）——
  已批量补齐 **53/54**：按录音卡 SN 分组缓存「SN → 命中配置」，同卡只定位一次、
  其余用命中配置复验，**94 次上游调用 / 约 1 分钟**（节流 550ms 对齐上游 QPS 2），只填空、幂等。
  归属分布：Amy 149 / Daniel 108 / Joyce 104 / Rin 54 / Joy 52 / Angela 30 / Elsa 27 / …
  剩 1 条（「日常闲聊与情感困扰讨论」）**13 套凭证逐个试都拉不到** ⇒ 保持为空、不猜。
  ⚠️ 顺带暴露：配置表「归属人」有 `孙旭峰` 与 `孙旭峰｜Richard` 两种写法
  ⇒ 按归属人筛选会出现两个孙旭峰（要合得先统一配置表写法，且不能用包含匹配）。
- 验证：api/web typecheck · i18n lint 544 条 + zh/en 对称 0 差异 · BUILD_ID `4pnG0Z2scKDRPEKI7bECR`
  （槽 3001/3101）· **A 用真实非管理员会话验收**（拿 13 套启用凭证逐个试上游定位归属 →
  用归属人本人的角色会话打开 ⇒ 断言四个字段落库补全，3/3 通过）·
  D 断言「已抓的不误报、上游无音频的不误标」。

### 2026-09-22 早 招生跟进「学生姓名」：修联动挂错列 + 改只读（`358a2ff`）

峰哥：「新建/修改时选完联系人之后，学生姓名来自联系人表的 `学生姓名 · xsxm` 自动带出；
直接显示就可以了、不可以编辑」。查下来是两个问题叠在一起 —— 一个是旧 bug、一个是缺能力：

- 🔴 **联动 patch 挂错了列 ⇒「选完联系人自动带出」一次都没生效过**（2026-09-19 引入）：
  `CrudPage.applyFieldChange` 只跑**当前变化列**自己的 `onChangePatch`，而它当时写在了「学生姓名」列上
  ⇒ 只有用户手改「学生姓名」才触发，还拿输入框里的姓名当联系人 id 去查，永远查不到。
  函数没写错、只是挂错列 ⇒ 类型检查 / 接口 / 构建**全都不报错**（静默失效的典型）。
  → patch 移到「关联联系人」列（选完联系人才是真正的触发点）。
- 🔴 **`CrudPage` 默认单行文本分支漏了 `readonly`**：列上写 `readonly: true`，表单里**照样可编辑**
  （textarea / link 两个分支各自处理了，只有默认分支没有）。补上只读展示分支后，顺带修正了
  考勤「审核状态 / 审核人」、上游账号「最近失败信息」同样的隐性不一致。
  ⚠️ 只读只影响**渲染**：提交时仍带上已存值（payload 取自表单状态），所以「系统带出的值」照常落库；
  **别**把它加进服务端 meta 的 `readonly` —— 那是**写入侧硬过滤**，加了反而一个字都存不下。
- **数据回填**（峰哥授权）：9 条历史招生跟进的「学生姓名」全空 ⇒ 从关联联系人的
  `自定义字段.xsxm` 落 PG 一次补齐（只填空、幂等）：`UPDATE 9` / 仍为空 0 / 与报名表不一致 0 / 重跑待填 0。
  快照 `/opt/acms/data/backfill_student_name_after_20260922-064430.json`（452 KB）＋
  回滚脚本 `rollback_student_name_20260922-064430.sql`（改动前该键全空 ⇒ 回滚 = `data - '学生姓名'`）。
- 验证：i18n lint 544 条通过 · web/api 构建通过 · BUILD_ID `4v7bciIf-o-_4754JjnPq`（槽 3002/3102）·
  **核线上产物**（不只看接口）：`source-followups/page-*.js` 里 `onChangePatch` 已落在 `weilingContact`
  列对象、`key:"学生姓名",…,readonly:!0`；只读 div 的样式模式在产物里出现次数 **2 → 3**
  （新增的正是这次补的分支）。
- 📌 **遗留（已告知峰哥）**：上游报名表 `xsxm` 有脏值 —— 本批 2 条填的是**手机号** `13482427338`。
  字段在系统内只读 ⇒ 这类只能去上游（卫瓴）修正，界面上改不了。

### 2026-09-21 中午（3）记录类型再加一项「学生沟通」（`4067252`）

峰哥：「下拉框再增加一个"学生沟通"，和刚开发这个 IDP沟通 功能一致」。同批口径照搬：

- 类型定义（`moduleKey` 同样**复用 `dailyFollowups`** ⇒ 现在 日常跟进 / IDP沟通 / 学生沟通
  三者共用一个权限点，凭日常跟进权限即可用，零角色配置）+ 字典种子 + 前端词表（复用 `COMM_WORDS`）+
  详情页 AI 摘要 kind + getnote 实体标签（`studentComm`）。
  语义写进注释：**学生沟通 = 与学生本人**的沟通，区别于「家校沟通」= 与家长的沟通
  （后者是家长端/学生端门户**唯一**会读到的类型）。
- 🔴 **生产字典这次不用我补**：查线上时已是 5 项且顺序正确（日常跟进 → IDP沟通 → 学生沟通 →
  家校沟通 → 学生观察）。写文件只有 `PUT /dictionaries/:key` 一条路径 ⇒ 说明是**从界面改的**
  （与上一轮 IDP沟通 同样的现象，已如实问过峰哥）。我的补写脚本按「读现值 → 缺才插入」写成幂等，
  这次直接跳过、没有覆盖任何人工值。
- 顺带修一处测试写法：上一版把「持日常跟进权限 ⇒ 拿到哪几个类型」的清单**写死了**，
  这次加类型它就变红（红得对，但改起来是噪音）。改成**按类型定义动态推导**
  （期望值 = 所有 `moduleKey === 'dailyFollowups'` 的类型值），以后再加同类类型不必改测试。
- 验证（探针 0 残留，12 项全绿）：建「学生沟通」→ 读回类型正确、字段与日常跟进同构、按类型筛出、
  总行数 +1 · **无权限会话建它 403 且看不到** · **门户不外流（带阳性对照）**：
  门户能读到「家校沟通」探针、读不到「学生沟通」探针。
  300 项测试全绿 · BUILD_ID `no4OxW1x7bIiRiINMfupF`（槽 3002/3102）。
  📌 峰哥持续在用：学生记录已从 8 条增到 12 条。

### 2026-09-21 上午（2）学生记录新增「IDP沟通」类型 + 收起旧 IDP 沟通入口（`d2bf46a`）

峰哥确认方案（5 件全按建议）后开工。核心判断：

- **新增一个记录类型 = 5 处改动**，逐处都有「漏了会怎样」：① `packages/contracts/src/student-records.ts`
  的类型定义 ② 字典「记录类型」取值 ③ 前端 `columns.tsx` 的词表 ④ 页面写死的文案（副标题 + AI 提示语）
  ⑤ 核实字典持久化文件是否会覆盖种子。
- 🔴 **权限复用 `dailyFollowups`、不新造权限点**。技术依据：`typeAllowedValues()` 是
  「遍历 `typeModules` 的 `[value, key]` 再判该 key 的权限」⇒ **多个类型值共用同一个 moduleKey 是被支持的**。
  新造权限点的代价是「上线后除管理员外没有任何角色持有它 ⇒ 所有人都看不到这个类型」，
  且要加权限行 + 用 `subOf` 挂到菜单下（不挂则权限矩阵里没有这一行可勾）+ 权限版本 +1。
- 🔴 **`ownFields` / `legacyPath` 目前没有任何消费点**（字段显隐由前端 `showIf` 决定）⇒
  「与日常跟进完全相同」= **什么都不用加**；反之若字段不同，要改的是前端 `showIf`。
- **文案改为从类型定义动态生成**（`TYPE_LIST_TEXT`）：原来副标题与 AI 提示语两处都写死三类，
  这次就得记得改两处 —— 改成动态后，以后再加类型不必碰文案。
- **收起独立的 IDP 沟通入口**（原来有**两个**界面入口，都清掉）：IDP管理页行级的
  「沟通记录 / 新增沟通」两个动作 + IDP方案详情页内嵌的 `<CommunicationManager>`；
  三个旧路由（列表 / new / [cid]/edit）改为 **307 重定向**到 `student-records?type=IDP沟通`
  （沿用项目做法：不用永久重定向，否则将来想挪回来很难纠正）。组件保留但标注不再挂载，
  后端 `IDP_COMM_META` 与表保留（表 **0 行** ⇒ 收起入口没丢数据）。
- 🔴 **踩到的真坑：字典持久化文件的结构是 `{dictionaries, fieldLevels}`**，
  检查某个字典 key 是否存在必须看 **`d['dictionaries']`**（我方案里查的是外层 ⇒ 结论写反了：
  以为「改种子会生效」，实际「记录类型」就在文件里、会覆盖种子）。线上仍是 3 项即由此而来。
  已走 `PUT /dictionaries/记录类型`（需系统管理员）补数据：**写前备份**、
  写后**接口 / 前端整表 / 文件三处回读**一致、字典 key 总数不变（93）、会话 0 残留。
  顺带查到：字典文件**只有** `PUT /dictionaries/:key` 与 `PUT /dictionaries/field-levels` 会写
  ⇒ 「线上字典值与种子不一致」时**重启不会自动修正**。
- 单测 +6（`test/student-records.test.ts`）：字典取值与类型定义**逐项且同序**一致 · 取值不重复 ·
  **每个类型的 moduleKey 都在 `MODULE_RESOURCES` 里**（否则该类型谁都看不见）·
  IDP沟通 复用 dailyFollowups（回归断言）· 未知类型返回 undefined ·
  **持日常跟进权限 ⇒ 同时拿到两个类型；只持学生观察权限 ⇒ 拿不到 IDP沟通**。
- 验证（探针 0 残留）：字典 4 项且顺序正确 · 建 IDP沟通 → 读回类型正确、字段与日常跟进同构 ·
  按该类型能筛出、总行数 +1 · **无权限会话建它 403、也看不到** ·
  **家长端不外流（带阳性对照）**：门户 `/portal/comms` 返回了「家校沟通」探针、**没有**返回
  「IDP沟通」探针（探针用 SQL 造，因为门户按 readonly 的「关联学生编号」过滤，接口写不进去）。
  300 项测试全绿（+6）· BUILD_ID `6JjmZ3VuxL8y96fpZ60sR`（槽 3002/3102）。
  📌 部署后**峰哥已自己建了两条真实 IDP沟通 记录**（张峻豪、马一然），主表现有 8 行。

### 2026-09-21 早（学年/学期筛选 + 两个弹出框 + 多科目建列，`11a004b`）

峰哥确认设计稿 v5 后一次做完四件事（同一处改动，分两次做要改两遍同一个文件）：

- **① 成绩册列新增「学年 / 学期」归属（读字典），页面顶部加两个下拉**。
  前提事实：成绩册列原来**没有**这两个字段、网格也只按班级筛 ⇒ 不是纯前端加下拉，
  必须给每一列记上它属于哪个成绩册。
  - 顶部 `学年 | 学期 | 班级`；前两个读字典「学年」「教学学期」（与「成绩批次」同一套取值），
    默认值按今天推断（`defaultTermOf`：8 月起算新学年、2–7 月为第二学期，**本地时区**）；
  - `GET /markbook/grid` 支持 `year`/`term`，**不传 = 不限**（老链接行为不变）；
  - 列的学期 = 「全学年」⇒ 该学年两个学期都算它。
  - 🔴 **未归属的历史列在任何筛选下都保留**（`columnInTerm` 兜底），界面用一条提示说明它们
    为什么一直在。这是本次最要紧的防线：加了筛选却把老列排除 ⇒ 页面"数据没了"、
    期末结转**静默少算**，且不报错。
- **②「新建考核列」改弹出框**（按「归属 / 考核内容 / 计分 / 可见性」四段）。
  顺手治掉两个老毛病：「编辑考核列内容不跟着列变」（内联面板可点 + 组件没 key ⇒ React 复用实例，
  `useState(col?.x)` 初值只在首挂载算过）；「类型留空被静默按权重 1 算」（弹窗底部写出将创建哪几列）。
  另加「保存后继续建下一个」。
- **③ 科目可多选：勾 N 个科目 = 建 N 列**（峰哥上一轮提的，这次一起做）。
  为什么不能一列挂多个科目：期末总评的幂等键是「批次 + 学生 + 科目」。
  展开规则（去重、≥2 个才加 `名 · 科目` 后缀、排序连号保证相邻、空串 = 未指定且只留一个）
  抽成纯函数 `subjectColumnDrafts`，**放在 `packages/contracts` 让前后端共用** ——
  前端要用同一份规则预览，复制一遍必然漂移（预览 3 列、实际建 4 列，两边都不报错）。
- **④「作业转成绩册」按钮放到「＋ 新建考核列」旁边，点击也是弹出框**。
  原来那套面板埋在整张成绩表**下面**（要滚到底才看得见），而且**有未保存改动时整块不渲染** ⇒
  老师以为「功能没了」。现在按钮常在，改在弹窗里提示「先保存再同步」；并加四步指示条
  （选作业 → 绑定考核列 → 预览差异 → 确认同步），把「先预览后确认」做成看得见的流程。
- **⑤ 期末结转 / 异常检测 / 科目候选按「批次同期」挑列**。结转原来只按班级取列
  （「考核日期落在批次区间内」那层过滤，老师多半不填日期 ⇒ 形同虚设）。
  空归属的列**仍参与**（保守，不静默少算）。`GET /exam-grades/subjects` 加可选 `year`/`term`。
- 验证（真实接口 + 探针，**0 残留**）：字典候选 · **勾 2 个科目 → 真的建出 2 列**（列名带后缀、
  排序连号 0/1、科目各归各的）· 换 2025学年 / 第二学期 ⇒ 探针列**不再出现**（筛选真的生效）·
  **未归属列在 2026学年 与 2019学年 下都仍出现**（兜底成立）· 不带 year/term 时列数 ≥ 按年筛的列数
  （向后兼容）· 科目候选含探针建的两个科目 · 探针删完后列数回到 0。
  产物层：`subjectColumnDrafts` / `columnInTerm` / `isUnassignedTerm` 在活跃槽 dist、
  contracts dist 里也有同一份；9 条新文案全在前端 bundle；`exam-grade` 里按批次同期取列已编译进去。
  **294 项测试全绿**（+15）。BUILD_ID `oyrucaMgJDUbI4bHz7N5o`（槽 3002/3102）。
  ⚠️ 仍未拿到页面截图（本机到站点 443 `Connection reset`，已知 EIP 间歇问题；服务器探活 200）。

### 2026-09-20 晚（成绩类型权重改成批量向导，`d478c5d`）

峰哥：编辑成绩类型权重录入太繁琐。原来是一条一条建（选班 → 选类型 → 填权重 → 保存 ×N），
一个班 7 个类型要点 20 多次，而且**容易漏** —— 漏一个类型，那一类就按缺省权重算，不报错、没提示。

改成**批量向导**（3 次点击配完一组）：
  ① 选班级（字典「当前年级」）→ ② 选考核类型组（停用的组不可选，与「停用=不许再选」一致）
  → ③ 该组下的类型**默认全选**（可全不选/逐个勾）→ 填「统一权重」→「填入选中项」→「保存」

刻意保留的四件事：
- **「填入选中项」只填输入框、不落库**，确认后才点保存 —— 批量写最怕一次改错一大批，
  所以预览与写入分开（与作业同步「先预览后确认」同一条原则）。
- 每一行**仍可单独改**，且改动时自动勾上该行（手都伸到这一行了，多半就是要写它）。
- 每行显示「本班已设 X」/「未设 · 按缺省 Y」⇒ 一眼知道**现在实际按多少算**。
- 保存**串行写**（同一张表并行 create/update 容易读-改-写互相覆盖）；未勾选的类型一律不动。

下方保留「该班已配置的权重」一览（含删除）：停用组里的类型在上方选不到，但仍可在这里删掉旧配置。

验证：组下拉 / 按组筛类型 / 按班级筛权重三条读链路均通 · 走了一遍真实的
「新建 → 读回 → 更新 → 读回 → 删除」⇒ 删除后列表**真的归零**（归档对界面生效）· 探针 0 残留 ·
apps/api 272 项测试全绿 · BUILD_ID `a1gxMnLdbpLCV1v7HEYPY`（槽 3002/3102）。
顺带体检：权重行的「类型」与考核类型表**全部对得上**（改名会让权重静默失效，这是最容易踩的一条）。

### 2026-09-20 下午（2）考核类型组 + 行为记录两处（`650665a`）

- **① 考核类型加一层「考核类型组」**（按峰哥要求，UI 参照「成绩等级」页）：
  页面**左侧维护组、右侧是该组下的类型**，与「成绩等级体系 → 成绩等级」同一形态，不另造布局。
  为什么要多这一层：同一批类型要能**整组开关**（换学期时一个开关搞定），
  逐个停用 7 个类型必然漏关，漏一个就会在成绩册下拉里留下过期选项。
  - 新表 `examTypeGroup`（组名称 / 状态 / 排序 / 说明），`ensureTable` 在 exam-grade 模块；
    考核类型表加 `所属考核类型组`（LINK），并在 meta 里登记 `linkFields`
    （通用 CRUD 才会把前端传的 id 归一成数组、列表里把 id 解析成组名）。
  - 端点 `/exam-type-groups` —— 权限用 `MODULE_RESOURCES` 的 **aliases** 收进
    `module:examTypes:*`（与 `/grade-scale-levels` 同一做法）：两级配置同属一件事，
    分开授权会出现「能改组、却改不了组里的类型」这种半开门。
  - 🔴 **存量类型必须回填**：此前 7 条类型没有组字段，而右侧按组筛 ⇒ 看着像被删了
    （实际还在被成绩册引用）。`backfillExamTypeGroup()` 幂等地把它们迁进自动建的「默认分组」，
    失败也不阻塞启动。
  - 🔴 **停用的语义 = 不许再选，不是历史作废**：组停用 ⇒ 其下类型不进候选
    （`listTypeOptions` 按组状态过滤，覆盖成绩册建列与成绩类型权重两个下拉），
    但存量成绩册列照常按权重与颜色计算 —— 否则一停用就把期末总评改了。
    组表读不到时**不过滤**（宁可多给候选，也不能空下拉）。
  - 组下还有类型时**不允许删组**（删了那些类型变「无组」，页面看不见却仍被引用）。
- **② 行为记录两处**：
  - 学生下拉**带英文名**（`中文名 / 英文名`）：本校大量学生以英文名相称，
    只显示中文名就得靠记忆对照，选错学生的代价是一条行为记录挂在别人名下。
    ⚠️ **筛选项仍用纯中文名**：通用 CRUD 的 `<字段>__has` 拿筛选值与 link 解析后的展示值比，
    传双语文案会**静默筛出 0 条**（不报错）⇒ option 里额外带一个 `name` 专供筛选。
  - 「行为分类」从自由文本改**读字典**，初始三档：课堂纪律 / 作业提交 / 文明礼貌。
    与「行为类型」（正向/负向）是两件事 —— 后者**参与告警累计口径**，改文案会让历史分成两拨。
- 验证（探针 0 残留）：组表建好 + **7 条类型全部归到「默认分组」（无遗漏）** ·
  探针建一个「停用组 + 组内探针类型」⇒ **该类型不出现在成绩册下拉**、其它类型数量不变 ⇒ 删除后
  组数回到原值 · 字典「行为分类」3 项、「行为类型」未被合并 · `/markbook-weights` /
  `/exam-types` / `/behaviour/records` / 成绩册网格均 200 · 产物层核对通过 ·
  apps/api **272 项测试全绿**。部署 BUILD_ID `_tkx5PorqX-AuoyL3vmS4`（槽 3002/3102）。
  ⚠️ 依旧拿不到页面截图（本机 443 已知 EIP 间歇问题；服务器探活 200）。

### 2026-09-20 下午（成绩册四处问题 + 等级序号深坑）

峰哥一次报了四个，逐个查到根因（两个是真 bug，一个是「有地方没维护」，一个是数据坑）：

- **① 考核列的「科目」改读字典「授课科目」**（`c14772a`）。原来候选 = **本班已有列**用过的科目
  ⇒ 等于「按班级配的候选」：新班 / 首次开某学科时**候选为空**，只能手打；而科目是
  **期末总评拆分的键**（总评是 学生 × 批次 × 科目 的快照），写法不一致（实测同班两列写成
  「数学课」和空）总评就拆成两份，且不报错。现改下拉、读字典「授课科目」（与教师档案同源）。
  「常用评语库」的科目一并改读同一份 —— 它的科目按**等值匹配**筛评语（批量评语按当前科目取句子），
  两边不同源就会出现「写了评语却永远筛不出来」。
- **② 编辑考核列：换了列、面板里的内容不跟着变**（`c14772a`）。根因是编辑器是
  **网格下方的内联面板**（`.mb-editor` 只有 `margin-top`，不是模态遮罩）⇒ 用户可以在它开着时
  点**另一个列头的「编辑」**或「新建考核列」；组件没有 `key` ⇒ React 复用实例，
  `useState(col?.x)` 的初始值只在首次挂载算过 ⇒ 面板里留着上一列的内容。
  按 `editing.col?.id ?? '__new__'` 给 key（**换列即重建**）。
- **③ 设了成绩目标，成绩册不显示**（`c14772a` + `b48f980`）。**先排除误判**：
  两条目标的班级与网格班级一致、两个学生都在该班名单里 ⇒ 后端早就匹配上了、达标也算出来了。
  真因是**展示只看「目标等级」这个名字**，而「学生成绩目标」页的表单**没有这个字段**
  （只有 目标分 + 目标等级序号）⇒ 名字恒为空 ⇒ 永远显示「未设目标」。
  修法：新增纯函数 `targetLabelOf()`（有名字用名字，否则按序号在**该生实际用的等级体系**里反查）；
  网格「有目标」的判据放宽为「名字/序号/目标分 三者有其一」，并把**目标分**显示出来。
- **③′ 深挖出的数据坑：本校等级体系的序号是 10/15/…/60，没有 1**（`b48f980`）。
  等级表实际是 `A=10 / A-=15 / B+=20 / B=25 / B-=27 / C+=30 / C=35 / C-=40 / D=50 / F=60`，
  而两条目标都填了 `目标等级序号 = 1` ⇒ 判定 `实际等级序号 ≤ 目标序号` **恒为假**：
  学生哪怕全 A 也永远「未达标」，等级名也反查不出来。**接口一切正常、不报错。**
  ⇒ ① 「目标等级序号」从自由数字改成**读等级表的下拉**（值=序号，label `A（序号 10）`），
  新增 `GET /markbook/level-options` + 纯函数 `levelOptionItems()`（按序号升序、按序号去重、
  多体系附体系名），接口失败退回数字输入；
  ② `GridSummary` 新增 `targetOrderKnown`，序号不在体系里时格子显示
  `序号 1 · 不在等级体系中 · 目标 99 分`（红标 + 悬停说明），不许「静默永不达标」。
- **④ 新建成绩目标时选学生自动带出班级**（`c14772a`）。成绩册取目标是**按班级过滤**的
  （`normClass(目标.班级) === 网格班级`），班级手选错 ⇒ 那条目标永远不出现，且不报错。
  `CrudPage` 新增列级开关 `studentClassAuto`：按**与后端 `classOf` 相同口径**（当前班级 → 当前年级）
  带出；只在学生变化时写、取不到就原样返回（绝不写空串把用户填的清掉），带出后仍可手改。
  `CrudPage` 另新增 `selectLabels`（值 → 显示名）：`select` 可以「库里存数字、界面显示人话」。
- 验证（真实接口 + 探针，探针 0 残留）：等级候选 10 项且**不含序号 1** · 峰哥两条目标现在
  判为「序号不在体系里」并显示目标分 · **探针建一条序号=10 的目标 → 等级名反查为 `A`、
  序号合法=True**（证明链路通）→ 删除后行数回到原值 · 类型候选 7 条、别的班与评语库页无回归 ·
  `/exam-comments` 200 · 探针会话 0 残留。产物层：`level-options` 路由 / `listLevelOptions` /
  `levelOptionItems` / `targetLabelOf` / `targetOrderKnown` 全在活跃槽 dist，前端文案在 bundle。
  apps/api **272 项测试全绿**（+9）。部署 BUILD_ID `nD12_DzdH07XC14krpa20`（槽 3002/3102）。
  ⚠️ 仍未拿到页面截图（本机到站点 443 `Connection reset`，已知的 EIP 间歇问题；服务器探活 200）。

### 2026-09-20 上午（下·2）

- **成绩册「新建考核列」的考核类型改下拉**（`e018b0c`）。峰哥的原问法是「应该读成绩类型权重里
  对应班级的考核类型吧？」—— 核实后是**对了一半**：
  - 权重表（`markbookWeight`）是**按班级配的**，生产只有「全球领航计划」配了 5 条，
    Pre-1 / Pre-2 / Foundation 等班一条都没有 ⇒ 拿它当候选，那些班的老师会看到**空下拉**，
    反而建不了列。
  - 正确分工：**「有哪些类型」由「考核类型」页（类型表）管；「每类在本班占多少分」由权重表管**，
    未配时回落类型表的缺省权重。所以候选取类型表（与「成绩类型权重」页同一份名单），
    并把**本班权重**标进选项文案 —— 峰哥要的「一眼看出这个班该用哪类」也就满足了。
  - 实现：`GET /markbook/type-options?cls=<班级>`（`items` 保持向后兼容，新增 `detail`），
    label 形如 `期末考试（本班权重 55）` / `日常练习（缺省权重 5）` / 追加 `· 不计入总评`。
    权重匹配口径与 `configsOf` 一致（`normClass(班级) === cls`），否则会出现
    「下拉说本班 20、实际按缺省 50 算」。
  - 为什么必须改下拉：类型名是**逐字匹配的键**，手打错一个字 ⇒ 该列的权重与「是否计入期末」
    **双双静默失效**。另加两条边界：类型已不在候选里时补一项标「已不在类型表中」
    （否则显示成「未指定」，让人以为从没设过）；类型留空不阻断保存但**显式提示后果**
    （按权重 1 计入期末总评）。
  - 顺带清掉 6 处「界面显示星号」：`examGrades` / `impersonate` / `apiTokens` 的几条 notice 写了
    markdown 加粗，而渲染位置是纯文本 ⇒ 原样显示 `**`。zh/en 各 6 处去掉星号。
  - 验证：不带班级的 items 不变（权重页零改动）· 全球领航计划的 label 显示本班权重
    （期末 55 / 实践 20）· **Pre-1 回落缺省且候选非空** · 给 Pre-1 配权重后 label 立刻变
    「本班权重 60」（口径一致性反证）· 建列落库且网格回传类型与颜色（`#36CFC9`）· 探针 0 残留。

### 2026-09-20 上午（下）

- **七个页面的下拉统一改读字典表**（`a266ee2` + `7ff8a4c`）：
  成绩类型权重（考核类型 → **读「考核类型」表**；班级 → 字典「当前年级」）·
  成绩批次（学年 / 学期 / 舍入口径 / 免考处理 / 缺考处理）· 学生成绩目标（班级）·
  学习成果（适用范围 / 适用年级）· 课程规划（学年原为自由文本 → 下拉；学期）·
  行为记录（行为类型）· 常用评语库（标签）。

  字典新增 8 个 key：学年（2020~2030学年）· 教学学期 · 舍入口径 · 免考处理 · 缺考处理 ·
  行为类型 · 评语标签 · 适用范围。

  四条判断（都写进了代码注释，避免下次重新想）：
  1. 🔴 **「学期」不复用，另立「教学学期」**：字典里已有的「学期」是学生档案口径
     （`2025春/2025秋`），教学域是「第一学期/第二学期」—— 共用一个 key 会让一侧下拉出现错值。
     且「教学学期」取值与成绩批次表的字段枚举保持一致。
  2. 🔴 **三个结转口径的字典值 = 服务端判据字面量**，所以种子**直接 import 常量**
     （`ROUND_MODES / EXCUSED_MODES / ABSENT_MODES`），并加单测断言逐一相等 ——
     手抄字符串会在常量改动后静默错位。
  3. 🔴 **「考核类型」读表不读字典**：它是真业务配置（缺省权重/计入总评/颜色），
     且第二层权重按**类型名等值匹配**；放进字典就是第二份名单，改名后权重静默失效。
     新增 `GET /markbook/type-options`（挂 `module:markbook:read`）而不是让前端直连
     `/exam-types`（那要 `module:examTypes:read`，配权重的老师会 403 → 下拉空白）。
  4. 🔴 **字典化本身带来新风险，已补防护**：档位进了字典 = 运营能改；谁把「不计入分母」
     改成别的写法，`=== '不计入分母'` 判假 ⇒ **免考反而被算进分母**（与预期相反）、不报错。
     新增纯函数 `pickMode()`，批次与全局设置读取侧都归一，非法值回落下一级。

- 验证（真实接口 + 探针，验完 0 残留）：8 个字典 key 取值逐一核对 · 类型候选 5 条与
  `/exam-types` 逐条对账 · 权重/批次/目标建改读回正确 · 带批次跑结转预览 200 · 清理干净。
  产物层：9 个 dictKey 全部进了前端 bundle、后端 `listTypeOptions` / `type-options` /
  `pickMode` / 字典新值都在活跃槽 dist。apps/api **263 项测试全绿**（+10）。
  ⚠️ 未拿到页面截图：本机到站点 443 仍 `Connection reset`（已知 EIP 间歇问题，服务器侧探活 200）。

### 2026-09-20 上午

- **补齐「有接口没页面」的四个成绩配置页**（`46f7560`，教学管理菜单下）：
  成绩批次 `/exam-batches` · 成绩等级体系+等级+绩点 `/grade-scales` · 成绩类型权重 `/markbook-weights` ·
  学生成绩目标 `/markbook-targets`。此前页面提示「先去『成绩批次』建一个」，而侧边栏里没有那个入口 —— 死循环。

  同时修掉三个「不报错但后果重」的问题：
  1. 🔴 **列编辑会静默清空字段**：后端 `saveColumn` 原先「没传 = 写空串」，
     而表单没渲染「科目 / 可见性 / 完成闸门」⇒ 老师只改一下权重，**科目等字段被一起清空**；
     科目没了，期末总评就拆不出科目。改为「未传的字段不动」，并把字段构造抽成纯函数
     `buildColumnFields` 以便单测（新增 8 项）。
  2. 🔴 **免考 / 缺考 / 字母等级 / 百分号录不进去**：前端把格子输入 `Number()` 掉 ⇒ `A`/`缺`/`免考`
     全变 NaN ⇒ 序列化成 null ⇒ 服务端按「未录入」**把格子删掉**。改为原样传字符串；
     格子也改成能显示「免考 / 缺考」（原先免考显示成空白、缺考显示成 0，看不出录上没有）。
  3. **`描述` 不返回** ⇒ 编辑列时初始化成空串，保存即清空。现在 grid 会返回。
  4. `ROLE_PERMISSION_VERSION = 4`：四个新模块各自独立权限点，legacy 继承口径
     （批次 ← `module:examGrades:*`，其余 ← `module:markbook:*`）。**这一步不能省** ——
     注册模块后 `moduleByPath` 会命中新 key、鉴权自动切到新点，不继承就会在升级瞬间
     让「考试与成绩」的批次下拉 403、成绩册的等级体系读取失败。

- 验证（造探针数据、真实接口、验完清理 0 残留）：等级体系+3 档绩点、批次（含数字阈值与 link 解析）、
  类型权重、学生目标全部可增删改查；**只改权重后科目/可见性/闸门/描述全在**；
  格子录入 `85` / `B`（折算 84.5）/ `85%` / `*`（免考，得分为空）/ `缺`（缺考，按 0 分）全部正确，
  体系里没有的字母 `Z` 报错且不落库。产物层核对：contracts（4 条菜单 + 权限点 + 版本 4）、
  api（`buildColumnFields`）、web 活跃槽（4 个页面名 + 新文案）均在。
  ⚠️ 未拿到页面截图：本机到站点 443 仍 `Connection reset`（已知 EIP 间歇问题，服务器侧探活 200）。

### 2026-09-19（下）

- **家长 / 学生门户落地**（#2，`1674d47` + `a469a0f`）：多子女切换 + 自助查询 + 可见性查询层。

  **一个必须说清的前提**：门户的「可见性开关」此前**没有任何消费方** ——
  `学生可见` / `家长可见` 只在 4 张表上（成绩册列、成绩册条目、行为记录、课时教案），
  而「成绩完成闸门」（成绩册列的 `完成日期`）在全仓**只被写入和读出，没有一处判定**。
  也就是开关一直能勾、勾了不影响任何门户展示，本次才真正接上。

  - 判据独立成 `portal/portal-visibility.ts`（纯函数，两端共用）。三条语义按生产数据现实定：
    **空串 ≠ 可见**（写入侧 `String(x ?? '')`，空串=没设置过 ⇒ 只有 `=== '是'` 才算开；
    写成 `!== '否'` 会把历史空值全当可见，没打算公开的成绩直接进家长手机）·
    **条目级空值跟随列**（作业同步写入的就是空串，否则一同步就清空家长端）·
    **闸门空值 = 不设闸门**（生产现存列全空，反过来解释会让上线瞬间家长端全空）。
    `todayStr()` 用本地时区（`toISOString` 是 UTC，东八区晚间少一天 ⇒ 只在夜间复现的错）
  - 查询层 `portal/portal-queries.ts` 两端共用：成绩改读**成绩册**（老「学业成绩表」无可见性字段，
    读它等于绕过开关）+ 作业（教案「作业布置」）+ 家校沟通记录；
    🔴 沟通记录必须卡 `记录类型 === '家校沟通'`，否则三合一后的教师内部记录会被推给家长
  - 家长端多子女：绑定新增**选填手机号**，填了则家长身份键取手机号 ⇒ 同一家长再绑第二个孩子
    自动进同一份名单可切换；不填则与历史行为一致（不会让存量家长突然多/少看到孩子）。
    切换只在会话 `studentIds` 白名单内（信前端 id 等于改个参数就能看别人家孩子）
  - 🔴 顺手修的静默丢数据：`submitFeedback` 原本写合并前的独立家校沟通表且**不写「记录类型」**
    ⇒ 三合一后家长提交的反馈在老师列表里**根本不会出现**（家长以为交了、老师永远看不到）
  - 线上验证（造探针数据 + 真实身份）：家长绑定 → 只能看到「家长可见=是且已过闸门」的格子
    （3 条探针成绩里只出 1 条）· 同号绑第二个孩子 → 子女数 2、切换器出现 · 越权切别人家孩子 → 403
    且当前子女不变 · 学生会话不受闸门约束（看到 3 条）但**不能调家长接口**（403）
  - ⚠️ 验证时发现并修掉一处只在页面看得出的错：成绩「满分」取成了条目的「百分制」
    （那是换算结果≈学生得分），页面显示「得分 88/88」⇒ 改为取列的「满分」，并补回归断言

- **修：权限矩阵里报表「无处可配」**（`8cd6512`→`02b3f1a`）：
  矩阵的行 = 「**菜单 → 模块**」按菜单 key 生成，而报表**不在侧边栏**（侧边栏只有「报表管理」一个入口）
  ⇒ 11 个报表权限点虽然登记进了 `MODULE_RESOURCES`、前后端也都按它判定了，
  **界面里却生不出对应行**，管理员根本没地方勾 —— 等于功能不可用。
  给 `ModuleResource` 加 `subOf`（没有自己菜单、但要在矩阵里占一行的模块，填父菜单的模块 key），
  报表挂到「报表管理」下渲染成 11 条缩进子行；子行不出「进入菜单」列（无独立入口）。
  ⚠️ 教训：**新增权限点只登记 `MODULE_RESOURCES` 是不够的** —— 还要保证矩阵能生成那一行。

- **修：权限矩阵里报表「无处可配」**（`8cd6512`→`02b3f1a`）：
  矩阵的行 = 「**菜单 → 模块**」按菜单 key 生成，而报表**不在侧边栏**（侧边栏只有「报表管理」一个入口）
  ⇒ 11 个报表权限点虽然登记进了 `MODULE_RESOURCES`、前后端也都按它判定了，
  **界面里却生不出对应行**，管理员根本没地方勾 —— 等于功能不可用。
  给 `ModuleResource` 加 `subOf`（没有自己菜单、但要在矩阵里占一行的模块，填父菜单的模块 key），
  报表挂到「报表管理」下渲染成 11 条缩进子行；子行不出「进入菜单」列（无独立入口）。
  ⚠️ 教训：**新增权限点只登记 `MODULE_RESOURCES` 是不够的** —— 还要保证矩阵能生成那一行。

- **报表按角色授权 + 下钻逐项对齐 + 角色显示统一 + 主题链接统一**：
  - 报表：11 张报表各一个权限点；🔴 迁移依据必须是 `module:reports:read`（12 个角色全有），
    用裸 `report:read`（只剩 5 个）会让 7 个角色升级瞬间静默丢掉所有报表；
    403 报错信息一并改成指向真正缺的权限点（原来指向旧的，照着勾永远勾不对）
  - 下钻对齐：卡片「25 组」点进去 59 条 ⇒ 四张卡都写明「点进去看 N 条」；
    `likely` 从累计口径改为**本档**；🔴 `weak` 不在合法取值里被静默忽略 ⇒ 返回**全表 3675 条**
    （比卡片大几十倍，比"少几条"更误导），现纳入合法档位且非法取值给空
  - 角色显示：真凶是身份模拟页直接渲染角色标识；另修 `RoleLabels` **加载失败会把空映射永久缓存**
    （整个会话所有角色退化成标识且不再重试）

### 2026-09-19
- **日常跟进 / 家校沟通 / 学生观察 三合一为「学生记录」**（`17bb486`）：
  峰哥要求把三个模块合并、用类型区分。核对后确认三者**本来就是「同一张表的三个视图」**——
  `RecordMeta` 的 `numbers/dateFields/statusField/defaultStatus/searchField/sortField` 逐字相同、
  共用同一批字典（沟通方式 / 家校闭环状态 / 信息敏感级别）、笔记转出的字段映射三条完全一致。
  实践活动另作判断（字段语义无对应、学生关联方式 by id vs by name、状态机不同），
  按峰哥决定**保持独立**，仅入口同组。
  结果：**一张表（沿用日常跟进表）+ 一个「记录类型」单选字段**。

  **权限设计（本轮最要紧的一处）**：新增 `RecordMeta.typeScope`（类型域）——
  - 读：`rowScopeFor` 追加「类型 ∈ 有权限的类型」，**服务端**过滤（分页与 total 正确），
    详情 / 导出 / 内存深筛消费同一份条件；`matchFilter` 补齐 `isempty`/`isnotempty` 算子，
    ⚠️ 这两个必须排在「want 为空 ⇒ 不限制」**之前**，否则语义反转成「谁都能看」
  - 进：`require()` 改为「**任一类型模块的同名动作**权限」。原因：合并前每个模块各有权限点、
    角色配置里存的就是那三个；主入口若只认 `module:studentRecords:read`，
    **没有任何角色能进入**（生产实测 24 人的主力角色 Phase1 只持有 `module:studentObservations:*`），
    等于把「看得见」降级成「看不见」，且不报错、只是空掉
  - 写：create 校验/补全类型 · update **不覆盖**已有类型（否则编辑别的字段会把一条
    「家校沟通」记录静默改成「日常跟进」）· 导入同样校验
  - 未打类型的记录按 `defaultType` 兜底 —— 没有它，漏打类型的记录会对**所有人**静默消失
  - 四个 path 指向同一张表：`student-records` 主入口 + 三个兼容入口（各自 `defaults` 打类型）

  **前端**：新增 `/student-records`（类型 Tab + 表头词表随类型切换「沟通人 ↔ 观察人」+
  表单 `showIf` 按类型显隐「家长三件套 / 观察类型 / 沟通方式」）；旧三路由（含详情页）
  重定向并带类型，**记录 id 原样保留** ⇒ 旧书签仍落在同一条；菜单 3 → 1。
  顺带修掉一个既有不一致：`AppShell` 侧边栏**漏了「学生观察」**（默认菜单里有），
  所以该入口一直没出现在侧边栏。

  **数据迁移**（`scripts/migrate_student_records.mjs`，幂等、带备份）：主表补 7 个字段
  （记录类型 / 家长 / 家长反馈态度 / 家长反馈 / 观察类型 / 关联学生编号 / 关联监护人）→
  现有 2 条记录补「记录类型」→ `nav_menu_config` 三菜单合一、`note_convert_config` 三条合一 →
  备份表 `bak_sr_*_20260919`。**三张源表都是 0 条记录（从未被使用）⇒ 没有搬数据的必要**；
  旧表原样保留，随时可回退。

  **验证**：本地 API 222 项测试全绿（新增类型权限 17 项）+ 前后端类型检查 + i18n lint；
  线上逐项 —— 管理员 2 条类型正确 · Phase1 200 但 0 条（只见其有权类型）· Phase3 403 ·
  Phase1 创建 403 · 非法类型 400 · 新建带类型与家长字段成功并已删除；
  浏览器实测 Tab 切换词表、旧地址重定向、侧边栏只剩「学生记录」、表单三类型显隐正确。

- **修 `push_via_curl.py` 的 rename 缺陷**：`git diff --name-only` 带重命名检测时，
  对「A 改名到 B」**只输出新路径**，旧路径不进 deleted 列表 ⇒ 远端残留旧文件、tree 与本地不一致。
  改 `--no-renames`（改名拆成「删旧 + 增新」两条）。同步清理了本次留下的残留文件。

- **交付顺序改为「commit → 推 GitHub → 部署 → 验证」**（峰哥定，2026-09-19）：
  **仓库是唯一事实源** —— 部署前代码必须已在仓库里，「部署完了还没推」属中间态、不算交付完成。
  ⚠️ 本次交付仍是**先部署、后推送**（沿用当时的习惯），最终仓库与线上内容一致
  （远端与本地 607 个文件零差异，tree 相同），**下一轮起按新顺序执行**。

- **学生记录列表「操作」列支持录音播放**（`0438571`）：峰哥需求，交互参考「我的笔记」。
  录音在**附件字段**「沟通附件清单」里（「我的笔记」转出时把音频 token 写进该字段）；
  线上实测形态：数组、每项 `{file_token,name,size,type}`、`type=audio/ogg`、目前每条 1 个。
  做法（复用既有能力，不另起一套）：
  ① 把「我的笔记」的行内播放抽成 `lib/rowAudio.ts` 的 `useRowAudio` —— **单 Audio 实例** +
  「同一时刻只播一个」（点另一行即切歌），⚠️ 不为每行渲染 `<audio>`：一页 20 行就是
  20 个播放器、每个挂着几十上百 MB 的音频源；hook 只收 `srcOf(row)`，笔记走专用接口、
  附件走通用 `/files/:token`；
  ② `isAudioFile`（MIME 优先 + 扩展名兜底）从 CrudPage **下沉**到同一模块，表单与列表共用
  一份判据；`audioAttachmentsOf` 的判据只认 `file_token`，与后端 `isAttachmentArray` 同口径；
  ③ 学生记录页用 `rowActionSlot` 渲染 ▶/⏸，`e.stopPropagation()` 防止误触行内「点击编辑」；
  ④ getnote 页改用同一 hook（删掉本地 50 行重复实现）。
  验证：附件接口 `Range → 206 + audio/ogg`、未登录 401；浏览器**真实点击**实测
  播放 / 切歌 / 停止三态都对；回归「我的笔记」播放正常。
  ⚠️ 未做（等峰哥确认，避免扩大范围）：详情页的附件仍是**下载链接**（CrudView 一处改动，
  影响所有模块的详情页）。

- **记录人/负责人默认取笔记归属人 · 招生跟进可播录音 · 详情页附件统一为播放器**（`5471bed`）：
  峰哥三组需求，一次交付。
  ① **「记录人 / 负责人」默认值 = 源笔记归属人**（原来取当前登录用户 ⇒ 管理员或同事代转
  别人的笔记会把归属人写成自己）。链路：`ConvertPayload.noteOwner`（getnote 传 `_owner`）→
  CrudPage 以 `{userName, noteOwner}` 调 `enrichPrefill` → 两个模块改成 `noteOwner || userName` 兜底。
  **历史数据一并回填**：`scripts/backfill_owner_from_notes.mjs`（幂等 / 只填空字段 / 带备份）——
  实测学生记录 2 条 → 徐洁｜Stefanie、招生跟进 1 条 → 吴洁｜Joyce。
  溯源链 = 转换记录.目标记录ID → 业务记录；转换记录.笔记ID → 笔记正文表.归属人。
  ⚠️ 不覆盖已有值：已有值可能是人工修正过的，冲掉不可逆。
  ② 招生跟进「操作」列支持录音播放（与学生记录同一套 `useRowAudio`；附件字段同样是「沟通附件清单」）。
  ③ 学生记录「记录人」列移到「时间」之后 —— 列表读成「谁的学生 → 什么方式 → 什么事 →
  什么时候 → 谁记的」，跟进人不是首要信息。
  ④ **详情页附件统一为内联播放器**：`AudioAttachment` 从 CrudPage 抽成独立组件，
  `CrudView`（只读详情）与编辑表单共用；原来详情页只给下载链接，想听录音得先下载再找播放器。
  **验证**：线上实测列序 / 回填值 / 播放（真实点击）/ 详情页 `audio` 元素；并构造 convert payload
  验证「归属人 → 记录人预填」链路 —— 登录用户是 Richard，表单里预填的却是**徐洁｜Stefanie** ✅

- **「取笔记归属人」推广到全部走笔记转换的模块**（`9dd4cf4`）：峰哥要求把上一轮只覆盖两个模块的
  规则铺开。改成 `noteOwner || userName`（归属人优先、登录用户兜底）的三处 —— 它们原本直接用登录用户：
  实践活动·活动负责人 · 阶段评价·评价人 · 校友跟进·跟进负责人；
  **新增**默认值的：会议纪要「主持人」「记录人」（原先留空）。
  ⚠️ 只默认**单人**字段：会议纪要的「参会 / 缺席 / 列席」是多人名单，默认塞一个人反而错；
  IDP 的「导师」是另一个人，也不动。学生记录的「责任人」同样未动
  （默认填会让所有记录的责任人变成同一个人，等峰哥确认）。
  **历史回填**：实践活动 / 阶段评价 / 校友跟进三张表**都是 0 条记录**（无历史数据）；
  会议纪要 2 条中 `rec_1b799e4f` 补为「姜龙女｜Emily」，另一条的源笔记**不在正文表里**
  （查不到归属人）⇒ 保持原值「孙旭峰｜Richard」不动。
  验证：线上列表显示正确 + 构造 convert payload 实测「主持人 / 记录人」均预填为笔记归属人 ✅

- **列表/表单重排 + 详情页按类型显隐 + 笔记标题与创建时间进预填**（`1fcb119`）：
  峰哥第二批需求（方案已确认后逐条落地）。
  ① 学生记录列顺序改为 **主题 │ 学生 │ 记录类型 │ 方式·观察类型 │ 时间 │ 记录人 │ 操作** ——
  **列表顺序用 `listOrder`、表单顺序用数组顺序**，两者解耦（混用一套就既满足不了
  「列表里主题第一」又满足不了「表单里主题排在类型/学生之后」）；**主题**列点击进**本条记录**
  详情，**学生**列改为进**学生档案** `/students/<id>`（依赖「关联学生编号」的 `__link`）。
  ② **UI 统一**：三种类型同一套骨架 + 按类型显隐 —— 表单用 CrudPage 既有的 `section` 分四块
  （基本信息 / 沟通内容 / 家长反馈（仅家校沟通）/ 跟进闭环）；🔴 **详情页（`CrudView`）也按
  `showIf` 过滤字段** —— 这才是「风格不统一」的真正症结：原先详情页把 20 个字段全摊开、
  一半是空的；顺带修 CrudPage 的 section 索引错位（它比的是 `formCols`，而遍历的是
  `shownCols`，有隐藏字段时标题会重复或缺席）。
  ③ 笔记 → 记录默认值：**主题 ← 笔记标题**、**时间 ← 笔记创建时间**（毫秒 → 本地时区 datetime）。
  ⚠️ 这两项**先塞进 values 再走 `enrichFromNotes`**，不是当 `defaults` 传 ——
  defaults 优先级最低（只在正文什么都没抽到时才填），而峰哥要的是「笔记标题就是主题」，
  标题比从正文正则抽的碎片可靠。
  ④ 招生跟进「负责人」字段开 `form`（原先只配了 listOrder，**表单里根本看不到**），排在活动类型之后。
  ⑤ 历史数据补齐 `scripts/backfill_note_fields.mjs`（幂等 / 只填空 / 带备份）：学生记录 2 条 +
  招生跟进 1 条的主题与时间；「关联学生编号」按**学生姓名精确匹配学生档案**回填
  （王逸凡→`recvtZbIHswfsW`、薛炜衡→`recvtZbIHsUOKI`）。
  ⚠️ 时间字段登记成 `type=5`，但**库里存的是毫秒**，回填必须按毫秒写。
  验证：线上实测列序 / 链接跳转（主题→本记录、学生→档案）/ 详情页过滤（日常跟进 17 个字段、
  无家长三件套与观察类型）/ 招生跟进表单出现负责人 / 构造 payload 实测预填
  （主题=笔记标题、时间=2026-09-19T11:44 本地时区、记录人=笔记归属人）✅
- **详情页加分区标题**（`6bbf66e`）：峰哥看过对比图后定「加」。
  `CrudView` 复用列定义里已有的 `section`，插跨整行的标题 —— 三种类型分别是
  3 区（基本信息 / 沟通内容 / 跟进闭环）与 4 区（多一个「家长反馈」，仅家校沟通）。
  ⚠️ 与 CrudPage 表单侧**同一套索引判据**：比的是 `cols[ci-1]`（showIf 过滤后的实际渲染序列），
  不是原始 `columns[ci-1]` —— 学生记录是「一份列定义按类型显隐」，拿未过滤序列比会索引错位，
  表现成标题重复或该有却没有。影响面：全站只有学生记录用了 `section`，其他模块详情页零变化。
  验证：临时建「家校沟通」「学生观察」各一条探针记录实测 —— 家校沟通 20 字段 4 区且含家长三件套、
  学生观察 17 字段 3 区且无家长无沟通方式（表头用观察类词表：主题 / 观察时间 / 观察人），
  日常跟进真实记录 3 区 + 内联播放器正常；探针记录已按**库内真实 id** 删除并复核为 0 残留 ✅
- **计划真源三处同步**：核对 GitHub Issues，关闭两条「PLAN 已标完成且记录里有上线依据」的
  —— **#5 考试与成绩**（`3c87e4c` + `eb3a423`）、**#6 身份模拟**（`c98911c`，另补了历史查询页与
  只读白名单）；均附上线依据评论后关闭。仍开放 4 条且与 PLAN 一致（#1 教学班数据补录 /
  #2 家长学生门户 / #3 AI 路由用户管理 / #4 首页 logo 重传，PLAN 里都是「待开始」）。

- **会议纪要表单改版：部门多选树 + 可搜索多选 + 选部门自动联动**（`5ecd85e`）：
  峰哥要求「部门可多选（部门树 + 复选框 + 级联）· 参会/缺席/列席/可见用户 UI 重做 ·
  整个页面参考学生记录重新设计」。改版 UX 见本节图；**真正的难点在一条连带改动**：
  「部门」从**部门名**改成 **od-id 数组**后，旧判据 `data->>'部门' = '学术轨'`（等值）
  会变成拿 `["od-…"]` 这段 JSON 文本去比 ⇒ **永远不命中** ⇒ 记录对同事全部隐身，
  而创建者靠「创建人ID」那条独立分支还能看到 ⇒ **不报错、列表也不空得可疑**。
  所以判据同批改造（不能拆开做）：「部门内可见」与「指定部门可见」**合并成同一支**
  （`可见部门 contains 我的部门范围(id)`），用 id 而非部门名（多值字段只能 contains 子串匹配，
  部门名会串台：当前 9 个名字恰好互不为子串，加一个「教学中心」就会撞「教学管理中心」）。
  ⚠️ 一处行为差异（安全方向不变）：部门的**下级**现在能看到上级部门的「部门内可见」纪要 ——
  与文案「本部门（含下级部门）的人能看到」一致，此前是漏的。
  前端新增两个控件：`DepartmentMultiTree`（勾上级=整棵子树，取消下级→上级**半选态**）、
  `SearchMultiSelect`（已选留上面、候选收进可搜索下拉）—— 会议纪要四处字段原来合计
  **112 个复选框**（表单被撑到三屏），现在四处都是普通输入框；顺带影响邮件账户「关联用户」1 处。
  新增 `CrudColumn.onChangePatch`（字段值变化时顺带改别的字段）。
  联动规则：选部门 → ①该部门（含下级）成员自动进参会人员 ②可见范围置「部门内可见」
  ③可见部门=所选部门（就是峰哥要的「可见部门 = 部门里填写的部门」）；
  **手动删掉的人不会因改部门被加回**、**用户自己改过可见范围就不覆盖**。
  验证：apps/api **227 项全绿**（判据测试新增 7 项）；线上实测表单五区/原生复选框 84→0、
  勾「学术轨」→ 三子中心级联 + 参会人员自动带出 12 人、保存后**可见部门自动等于所选四部门**；
  权限视角换真实身份实测 —— 钟慧婷（教学管理中心，子树内）看得到、Arete Developer
  （Arete AI Lab，子树外）0 条看不到、管理员豁免；探针记录已删净（0 残留）。
  存量 2 条记录的「部门」已由 `scripts/backfill_meeting_dept_ids.mjs` 转为 id 数组。

### 2026-09-18
- **录音全链路打通：转出带录音 + 列表行内播放 + 容器按文件头嗅探**（`6b7186b`）：
  峰哥报 4 件事，逐条落地 ——
  ①「部分音频没法播放」根因 = 录音落库时 MIME 与扩展名**写死 `audio/ogg`**，而实测
  554 个录音里 **40 个实际是 MP3**（另有 29 个 m4a / 1 个 webm）；浏览器按 `audio/ogg`
  解 MP3 字节流直接解码失败。新增 `file-storage/audio-format.ts` **按文件头嗅探**真实容器，
  播放接口动态给 Content-Type；存量 40 个文件的元数据 + 正文表展示信息用
  `scripts/fix_note_audio_media.mjs` 修正（残留 0，m4a/webm 只报告不改 —— `ftyp` 分不出
  音频轨与视频轨，硬改会让真视频丢画面）。
  ② 播放链路补 **Range（206）** 与音频 `inline`（新 `file-storage/byte-range.ts`）：
  录音最长 3 小时 / 单文件 168 MB，全量返流会让进度条拖不动、**Safari 直接拒播**。
  ③ 「我的笔记」**列表接口返回每行 `_audio`**（主键批量查，不打上游、不受 QPS 2 限制），
  `CrudPage` 新增 `rowActionSlot` 插槽，操作列放「▶ 播放 / ⏸ 停止」（单 Audio 实例、
  同一时刻只播一条；录音类但未落库的显示禁用按钮 + 原因提示）。
  ④ **转出时带上录音**：`NoteConvertTarget` 新增 `audioField`（11 个模块默认映射），
  会议纪要新增「会议附件」列承接，转换配置页可编辑；附件字段里音频渲染内联播放器。
  **历史数据**：已转出的 5 条业务记录用 `scripts/backfill_convert_audio.mjs` 补上录音
  （token 复用、不复制文件）；另修**正文表归属缺失**（66 行 `来源配置ID` 为空，42 行从
  快照表回填）—— 这正是 8 条贺嫣的笔记补抓音频报「权限不足」的根因（凭证解析退化成
  管理员自己的 Key ⇒ 上游 10008）。
  **顺带挖出一个潜伏已久的读侧 bug**：**附件字段读出来恒为空**。`toText` / `toStringArray`
  是按**富文本数组**语义写的（取每项 `.text`），而附件项只有 `file_token` ⇒ 非空附件被拍成
  空串；更麻烦的是拍平发生在**两处** —— `SqlStore.normalize` 按字段元数据格式化
  （「沟通附件清单」历史建表时被登记成 type=1 文本 ⇒ 走 toText），以及
  `toFlatRecord` / `buildWriteFields` 的展平。判据 `isAttachmentArray` 下沉到
  `@acms/base-adapter/convert.ts` **一份**，两条链路共用，避免「改了一处另一处还在拍平」。
  此前全站没有哪个模块真存过附件（家校沟通 / 学生观察 0 条，日常跟进与会议纪要各 2 条
  且都是当天新写入），所以这个 bug 一直潜伏到「转出带录音」才第一次暴露。
  **结果**：音频 533 → **546 条 / 312.7 小时**，待抓 0；峰哥点名的贺嫣「IDP一对一沟通：
  个人情况、职业规划与在校学习安排」（19.8 分钟）与蒋潘云「付庄浩Preto升学沟通面谈记录」
  （7.4 分钟）均已补齐。
- **数据范围「一个都不选」由「看全部」纠正为 fail-closed**（`138143e`）：原语义
  fail-open，配错角色等于全放开；引入 `SCOPE_DENY_ALL` 标记，`studentInScope` 恒 false，
  10 处调用点零改动。实测 Phase1 82 / 只配 Pre-1 → 33 / 空范围 → 0 条。
- **「数据密级」配置页上线**（`55dd88e`）：字段级密级的唯一配置入口（adminOnly），
  含用户密级分布 / 按模块分组的字段密级矩阵 / 保存前 diff / 打码预览；
  可配模块 4 → 18 个（`FIELD_LEVEL_MODULES` 刻意放 `dict.data.ts` 而非
  `module-permissions.ts`，避免把 DB 表 id 泄到浏览器 bundle）。
- **角色页保存按钮点不动 + 「密级上限」长期不生效的误导**（`68874aa`）：
  `dirty` 手写比较漏了 `dataScope`；`auth.service` 把空密级写成 `'L1'` 导致个人值永远有值、
  角色上限的回退分支永远走不到 —— 抽出 `engineLevelOfUserField()`（空 ⇒ undefined 回退角色、
  已知值 ⇒ 覆盖、无法识别 ⇒ L1 保守处理）。
- **`/data-levels` 页面不停闪**（`3bf8a04`）：`useTl()` 每次 render 返回新函数，而页面写了
  `useEffect(..., [tl])` ⇒ 死循环（服务端铁证：同秒最多 49 次 `field-levels/catalog`）。
  改为 `useCallback([locale])` + `useRef` 持最新 labels；同类写法生产共 6 处一并核对。

### 2026-09-17
- **按钮权限全站收口 + 知识库（笔记）三项改造**（`9bc6a9a` / `5c86259` / `05ff828`，Issue #8）：
  峰哥报障「曹德强没有知识库设置 / 我的笔记的编辑删除权限，却仍看得到按钮」——
  查下来**不是权限配错，是全站 59 处 `CrudPage` 里有 33 处没传 `moduleKey`**，
  而 `modOk` 在没传时直接返回 true（= 完全不控），这些页面对所有角色都显示新建/编辑/删除，
  点下去才 403。已给 32 处补上（剩 `/venues` 是孤儿路由，无菜单无入口，留待决定是否下线），
  并把 `modOk` 改成「只对模块 `actions` 里确实声明过的动作生效」，避免误藏按钮
  （`aiAgents` / `getnote` / `users` 的 actions 不含 export，硬判会让导出对所有人消失）。
  **安全性依据**：后端 `require(user, action)` 用的也是 `module:<key>:<action>` 同一权限点，
  所以「按钮隐藏 ⇔ 接口 403」天然一致。另三项：**笔记正文落库**（新表 `noteBody`，
  总结与原始记录分列存；与快照表分表，避免列表查询多拖几 MB）、**「重新收取」**
  （`POST /getnote/refetch-bodies`，异步 + 进度轮询 + 按笔记 ID 幂等）、
  **知识库配置改多用户关联**（新增「关联用户」字段，可见性判据抽成唯一一处
  `sourceVisibleTo()`，列表 / 详情 / 写鉴权 / 笔记收窄全调它）。
  生产实测：管理员见 17 条配置 / 曹德强只 1 条、越权读写均 403、我的笔记 482 vs 105、
  重新收取 9 条 stored=9 failed=0、9 个页面回归全 200。
  ⚠️ 部署时踩到一条重要事实：**生产 `SQL_TABLES=*`，所有表都路由到 PG，飞书 Base 只是历史镜像**
  —— 字段元数据必须写进 `acms_fields`、数据必须写回 `t_*.data` jsonb，改飞书对运行时零影响。
- **知识库两页修补：配置页分页回归 + 四处筛选**（`0c374b6`）：峰哥报「配置页只显示 10 条」
  ——真因是**上一轮把可见性过滤放进 `list()` 的后置步骤**，过滤在分页之后只能把结果收敛成单页
  （`total = 本页条数`、`hasMore = false`）⇒ 前端分页条永远 1 页、第 2 页起的数据翻不到。
  改为在 `RecordMeta` 上声明 `rowScope`（条件进 SQL WHERE），total/分页恢复准确。
  同批：配置页去掉「归属人」筛选、加「配置名称」（模糊）与「用户」（`关联用户__has`）筛选，
  `pageSize` 调到 50；「我的笔记」补 `来源`/`配置名称`/`归属人`/`标签` 四个服务端筛选
  （此前这些筛选控件发出去的参数被接口静默忽略，看着有 UI、点了没反应），
  并把「来源/标签」的拆分口径抽到 `packages/contracts`（前端展示与后端筛选共用一份）。
- **修复「我的笔记」列表恒为空**（`5c58639`，Issue #11）：峰哥报「刘佳音｜Joy 登录后我的笔记
  没有记录，系统管理员能看到她的笔记」。根因是**本人凭证源被可见源白名单丢弃**——
  她在向导页填过个人凭证、且与自己的配置是**同一份 API Key**，`collectAllNotes` 按 Key 去重时
  个人凭证源先占住 `seenKey` ⇒ 配置源被跳过 ⇒ 笔记全挂在 `_sourceRecordId=''` 上 ⇒
  严格白名单（`Set.has`）一个都不命中 ⇒ 恒为空；管理员因 Key 不同不受影响，故表现为
  「自己看不到、管理员看得到」。判据抽成纯函数 `noteInScopedSources()`（**空 recordId 必须放行**，
  它是本人凭证源、只含自己的笔记），并顺带让非管理员**只拉「我可见的源」**——
  原来每进一次页面要打完 12 个启用源（24 秒，且消耗**所有同事**的上游额度）。
  生产复验：刘佳音 **0 → 50 条、24s → 3.4s**，其余 11 人逐个实测全部正常（无一人为 0），
  管理员 575 条不变；新增 13 项契约测试，全量 api 128 + domain 17 全过。
  顺手修 `matchFilter` 漏认 `<字段>__link` 的既有隐患。线上实测：配置页 `pageSize=10 → total=16/hasMore=true`
  且能翻页；Elsa/Rin/Ruby 各只看到自己那 1 条配置与其笔记（25/47/6），标签筛选 12/2/0。
- **角色管理菜单顺序对齐系统菜单**（`129fd58`）：矩阵/列表视图的菜单分组与排序改为**与侧边栏同源**
  —— 原先按 `section` 首次出现顺序分组、且完全不按 `order` 排序，导致教学管理/AI 路由整组排到最后、
  分组名与侧边栏不一致（`知识库` vs `知识笔记`）。新增 `buildSystemSections()` 复刻 `AppShell.tsx`
  的分组算法（menu-groups 定分组与顺序、`order` 定组内顺序、`section` 同时比 key 与 label、
  未登记 section 兜底成组），矩阵/列表/白名单折叠区三处共用；adminOnly 18 项统一收进末尾
  「系统管理员专属」分区。用生产数据离线核对（抠源码真函数 + 11 类断言）与线上浏览器双向验证均一致。
- **笔记数据全量落库 + 报表口径与两个新维度**（`6be329f`）：峰哥报六项，逐一用生产实测坐实
  —— ①「配置名显示 1234567 / 看不到某人笔记」同一根因：映射表 88 行挂在**已删除的配置**上、
  157 行重复，显示层解析不到就**回退历史名**，而筛选下拉只列当前配置名 ⇒ 一条都筛不出来
  （Angela 的 19 条其实一直在列表里）。修法：映射主键改**笔记ID**（upsert 幂等）+
  一次性清理（688 → 580 行，配置名刷新为当前值，原表备份在 `bak_notemap_20260917`）。
  ② 字段落库：快照表补 标签/标签类型/归属人ID/笔记类型/来源/总结/子笔记数
  （原仅 8 字段、`acms_fields` 0 条元数据），正文表补 标签/来源/录音时长/附件数/录音卡SN ——
  实测上游**列表接口就返回总结与标签**，这两样零额外额度；570 条正文一次性补齐（QPS 2）。
  ③ 报表「按人/按来源」改为**按 ID 归并 + 名字实时解析**（改名残留会把同一配置拆两行）。
  ④ 新增「笔记统计 · 按标签」（多标签分别计入、排除来源标签、system 标签默认隐藏）
  与「招生分析 · ④ 按状态」（1 已认领 / 4 待分配 / 0 待认领（公海），含状态×阶段分布与下钻）。
- **角色管理页重构：「菜单可见性」并入矩阵 + 列表视图按模块罗列**（`1368b73`，Issue #8 后续）：
  先量化确认重叠 —— 菜单 76 项与模块 71 个**一一对应**（67 同名 + 4 走 `MENU_KEY_ALIASES`），
  矩阵的 `enter` 列恰好全覆盖；无对应模块的 5 项全是 `adminOnly` 或走独立权限点
  （`system-monitor`→`admin:monitor`、`aiDocs`→`ai:chat`），不必造伪模块。
  矩阵的行改为**菜单项**（按菜单分组分区），«进入菜单»列按三类行分别落到 `module:<key>:enter` /
  独立权限点 / 「—（仅管理员可见）」；删除独立的「菜单可见性」表格，但**「菜单白名单」不删**
  —— 它与 `enter` 不是同一层（前者是额外收敛，且「只有该角色所有角色都配了白名单才生效」），
  收进折叠区零行为变化。另定位列表视图的真病因：其渲染里把 `module:*` **全过滤掉**，
  与矩阵**零重叠**（用户说的「不完整、对不上」完全准确），改为按模块罗列 + 底部「兼容权限点」折叠区。
- **权限判据双轨制收口（收回越权）+ 菜单白名单修复**（`fd1f306`，Issue #9）：峰哥报「曹德强没有学生档案
  修改/删除权限却能看到按钮」+「Angela/丁懿 只能看到报表」。查下来是**同一类结构缺陷**：
  系统并存两套判据 —— `module:<key>:<action>`（角色管理矩阵编辑的、通用 CRUD 后端也用的）
  与 legacy（`student:write`/`getnote:write`…，只有少数**自建 controller** 在用），
  而矩阵只能编辑前者 ⇒ 界面与实效脱节。用**零副作用**请求实测坐实：曹德强(Phase1) 新建/编辑/删除
  学生档案分别返回 400/400/404（= 权限检查已通过，只因 payload 空被业务校验拦下），
  对照「教师本人」三项全 403 —— 即峰哥当初在矩阵里取消勾选，**权限根本没被收回**。
  已把 12 处自建 controller 的判据统一切到 module（student / getnote / weiling / dashboard /
  student-360 / reports / mail-archive / partnership / attendance / export / monitor / user /
  wechat-binding），前端 `/students` 补按钮门控（此前该页**一个权限判断都没有**）。
  菜单侧：Phase2/3 的白名单只有 3 项且其中 2 项缺 enter（白配）⇒ 把 24/23 个已授权菜单卡成 1 个，
  已清空白名单回自动模式；Phase1 白名单里 2 项缺 enter 的已补权限。
  线上实测：曹德强三项写全 403、丁懿/贺嫣恢复正常；菜单可见数 曹德强 9→11、丁懿 1→24、贺嫣 1→23。
- **会议纪要可见范围权限**（`2f5f471`，Issue #10）：会议纪要从「有模块权限就能看全部」
  改为按创建时选择的可见范围做行级隔离（**公开 / 部门内可见 / 指定用户可见 / 仅自己可见**），
  并支持「**部门领导看下属所有部门**」。判据挂在 `RecordMeta.rowScope`（覆盖 list / detail /
  export / 内存深筛；update / archive 因先调 detail 而自动受保护），豁免角色
  `系统管理员` + `院级管理`（= 公司最高领导人）。
  **「部门领导看下属部门」没有单独分支** —— 它被吸收进「我的部门范围」＝
  （我所属部门 ∪ 我担任负责人的部门）各自展开子树，负责人只是拥有更大的部门集合；
  部门负责人取自部门表 `leader_user_id`（实测存的是 **openId**，可直接与会话比对，无需映射）。
  ⚠️ 三条硬约束（已回写技能 `acms-row-scope` 与 memory）：
  ① **空 `value` 数组 = 不过滤 = 全部放行**（引擎两侧都把空数组当「没有条件」：
  `matchFilter` 返回 true、`buildCondition` 返回空串）⇒ 运行时算出的集合为空时，
  **整条 OR 分支必须不加入**，否则「部门内可见」会静默退化成「全员可见」；
  ② **`rowScope` 只能过滤 jsonb 字段**（条件一律 `data ->> $n`），**物理列 `created_by` 参与不了**
  ⇒「仅自己可见」必须把创建人冗余成 jsonb 字段「创建人ID」（create 钩子自动写；创建人不再变，
  所以「update 没有钩子」这个限制对它不成立）；
  ③ 飞书对**根部门**结构性地不返回负责人（`/contact/v3/departments/0` 的返回体**连 leader 键都没有**，
  且 name 为空串）⇒「公司最高领导人」只能用角色判，**不能用根部门负责人**
  （实际宋琼｜Sally 本身就是系统管理员，需求已被满足）。
  上线当天实测发现并修复：**创建人看不到自己创建的「指定用户可见」记录**
  （且 `create()` 写后回读 `detail()` 被自身判据挡住 ⇒ POST 返 **404**、前端误报「保存失败」
  而数据其实已写入，用户会重复提交）—— 修复为把「创建人 = 我」提升成**独立于可见范围**的顶层 OR 分支。
  验证：探针会话 4 条 × 4 身份全绿（越权详情 404 / 指定用户 200）、单测 22 项、i18n 校验、
  8 个页面回归 200，测试数据残留 0；**零引擎改动**。
  同批还顺手刷新了组织数据（刘玉蓉｜Yvonne 按飞书调整归入院长办公室 —— 她原先直属「公司」根部门，
  会让她的部门范围等于全部 9 个部门）。

- **会议纪要可见范围增补：新增「指定部门可见」+ 参会/缺席/列席改多选**（`00f9624`，Issue #10）：
  ① 可见范围从 4 档扩到 **5 档**，新增「**指定部门可见**」—— 可多选部门，**选中部门（含其下级）
  里的人都能看到**；新建时**默认选中「我所属的部门」**（新增 `GET /departments/my-departments`，
  以及 CrudPage 的通用 `createDefaults` 能力）。
  判据用 `open_department_id` 做 `contains`（**不用部门名** —— 多值字段只能子串匹配，
  部门名会串台（「教学」命中「教学管理中心」）；用 id 还顺带免疫部门改名）。
  **「我能命中的部门」= 我的部门 ∪ 其上级**，这样把纪要指定给上级部门时**整棵子树**都看得到。
  🔴 根部门「公司」的 `open_department_id` 是**字符串 `'0'`**，拿它做子串匹配会命中一切 ⇒
  从选项到判据**全链排除**（要全公司可见就用「公开」这一档）。
  ② 参会 / 缺席 / 列席人员改为**多选**（`person + linkMulti`，存姓名数组，与单选的「主持人」同口径）；
  CrudPage 顺带支持 `person + linkMulti` 与 `linkSource: 'departments'`。
  ③ 🔴 踩坑回写：三条人员字段**不能**登记进 `linkFields` —— 那个分支优先于 `multi`，
  会把数组读成「张三、李四」拼接字符串（实测 detail 直接返回字符串，前端回显与断言全跟着错）。
  验证：单测 29 项（+7）、全量 132 项、线上 4 身份可见性矩阵 **4/4**（含跨上下级）、页面回归 8/8、
  测试数据残留 0。
  ⚠️ 部署时又踩到「**打包失败但部署照跑**」：用分号串联时 `build_tars.sh` 被批量删除守卫拦下、
  tar 没重建，而 deploy 照跑、把**上一轮**构建发上了线（线上 buildId 与本地不一致，
  但部署脚本打印的「期望/实际」两边都是旧的，看起来一切正常）。
  已改为 `&&` 短路（BUILD_ID 不一致就不部署），并把这条与「scope: turn 下 export 也压不住守卫、
  要 `mv` 走 rm 目标」一起写进技能 `acms-prod-deploy`。

### 2026-09-16
- **令牌管理页缺陷修复**（`167edd1`）：峰哥截图报障「签发弹窗没有关闭 ×／到期日选一年报错」。
  根因是**页面默认值本身就是非法值** —— 日期框只有「日」精度、提交时按当天 23:59:59 算，
  而服务端上限写的是 `now + 365×24h`，差几个小时 ⇒ 用户什么都不改直接点「确认签发」也会被拒，
  整个签发功能不可用。修法：上限改按**日历**算（`now+365天` 那天的 23:59:59.999），
  抽成纯函数 `auth/api-token-expiry.ts` 并加 5 个回归单测；通过 `/state` 把绝对上限下发前端
  （绑 `input[type=date]` 的 max）+ 提交前本地前置校验。同批补上弹窗关闭 ×（含 Esc）、
  并修掉 7 个 i18n 漏 key —— 其中 3 个是令牌页自己的（界面直接显示 `apiTokens.cancel` 这类原始 key），
  另 4 个（`markbook.hwBindDone`、`common.{ok,failed,notSupported}`）是新检查扫全站揪出来的**同类线上缺陷**。
  `i18n_labels_lint.mjs` 已补上「`useTranslations(ns) + t(key)` 的 key 是否存在」这条检查，
  此前它只查 `tl()` 字面量，所以这类漏 key 能一路绿灯上生产
- **外部客户端接入上线**（`0b5cda5`，Issue #7）：让 WorkBuddy / Codex 等 agent 能调用 ACMS，
  不必再让人把浏览器 Cookie 抠出来塞进脚本 —— 新增 **API 令牌**（`acms-sk-` + 43 字符 base64url，
  记录 id = 明文 SHA-256，库里不存明文，明文只在签发响应里出现一次；默认只读 + 模块白名单 +
  IP 白名单 + 限流 + 过期 + 全量留痕可选）、**令牌管理页** `/api-tokens`（adminOnly +
  二次密码，与身份模拟同门禁）、**CLI / MCP**（`apps/cli/`，零依赖纯 ESM，经
  `apps/web/public/cli/` 分发 ⇒ 免改 nginx）、**`acms schema` 能力发现**（仓库无 OpenAPI，
  48 模块 / 338 路由，另留通用透传）。关键设计：令牌鉴权搭在 `SessionGuard` 里加分支而
  **不另起守卫** ⇒ `req.user` 仍是标准 `SessionUser`，学生行级数据范围 / `requireModule()` /
  审计操作人 / `currentActor()` 零改动自动生效；`impersonation-limit.ts` 提为通用
  `access-limit.ts`，靠 `kind` 区分「模拟对 `/impersonate/*` 永久放行」与「令牌硬拒」。
  生产实测：8 条硬拒路径全 403、只读写操作 403 `TOKEN_READONLY`、吊销后立刻 `TOKEN_DISABLED`
  （主动清缓存）、`curl -fsSL .../cli/install.sh | sh` 可用；81 个单测 + i18n lint 全绿
- **考试与成绩上线**（`3c87e4c`，Issue #5）：在既有 Gibbon 式成绩册之上补「结果产出」闭环 ——
  考核类型配置 → 成绩册录入增强（科目/列头色块/单元格状态/宽容输入解析）→ 期末总评结转
  （自归一化分母、免考不进分母、缺考按 0 进）→ 各科教师评语（批量页失焦即存）→ 成绩单 + PDF 导出；
  另含异常成绩审查（5 条规则，只提示不改分）与竞赛排名 / GPA。成绩单 PDF 走 pdfkit + 子集化中文字体，
  实测约 34 KB/份、服务器零系统依赖；新增 4 张表、15 个权限点
- **身份模拟上线**（`c98911c`）：系统管理员在 `/impersonate` 用二次密码（默认值可在
  `IMPERSONATE_PASSWORD` 覆盖）解锁后，可列出系统全部账号并以任意账号身份浏览 ACMS（排障用），
  顶部常驻金色横幅提示「以谁的身份、谁发起、剩余多久」、一键退出回管理员；
  四道防线 = 登录态 + 系统管理员硬校验 / 二次密码 / 同 IP 5 次锁 15 分钟 / 进入退出全程留痕；
  新增「身份模拟记录表」，并修掉两个隐患（模拟写登录日志污染活跃统计、覆盖目标用户 openid 反向索引）
- **考试与成绩 Phase 2 上线**（`eb3a423`）：`/reports` 新增「考试成绩分布」与「GPA 与班级排名」
  两张报表（**套学生行级数据范围**，含姓名分数明细不给越权看）；「成绩口径设置」做成
  模块第 5 个 Tab（复用系统配置表，批次字段优先的三级回落）；新增「常用评语库」表 + 页面
  （批量评语可一键追加）；**整班成绩单 ZIP 导出**用零依赖手写 zip 实现（生产不能装新依赖）；
  学生全景加「期末总评 / 成绩单」两个分区
- **身份模拟 Phase 2 上线**（同 commit）：**只读模式**与**模块白名单**（写进会话，
  由 SessionGuard 统一拦截，业务接口零改动；`/impersonate/*` 永远放行否则退不出去）；
  新增「模拟记录」页（只读审计，不需要二次密码）
- ⚠️ **一次部署事故**：`build_tars.sh` 的批量删除被沙箱守卫拦截 → tar 未重生成 →
  部署上传旧 tar，而 `| tail` 吃掉了退出码，表面"成功"实则生产仍是旧版。
  判据 = BUILD_ID 与本地 `.next/BUILD_ID` 不一致。之后部署都要核对这一项

### 2026-09-14
- **考勤终态审核上线**：学生考勤记录「待审核 → 已通过 / 已驳回」（单条 + 批量，驳回必须填理由），
  权限点 `attendance:approve`（系统管理员 / 院级管理 / 教务）；审核人 / 审核时间 / 审核意见落库并写审计日志
- **出勤率报表上线**（`/reports` 第 8 张报表「考勤分析」）：应出勤人次 / 实到 / 出勤率 / 迟到 / 请假 /
  缺勤 / 异常，按班级 · 年级 · 学生排行 · 按日/周趋势，KPI 可下钻名单；
  **只统计「已通过」终态记录**，未审核与已驳回单独计数不进分子分母；
  「计入统计=否」的考勤码整条排除；码表认不出的码单独计数（不猜）
- **作业 → 成绩册联动上线**：选班 → 选作业 → 预览 → 同步（未完成 / 无提交**留空不写 0**，
  得分优先取已批改的提交分数、无分数按满分折算）；列 ↔ 作业用列上「关联作业」自然键绑定，
  列头显示完成率；预览与写入共用同一份计划器，二次同步幂等（跳过已存在）
- **联系人去重报表上线**（`00a518b`）：50 组 / 109 条疑似重复，支持分组证据、建议保留、导出 CSV
- 修复通用 CRUD 文本筛选一律等值匹配的问题（`1792e25`）—— 全站 8 处文本筛选恢复模糊匹配
- **修复权限自愈缺口**：系统管理员权限在「保存角色矩阵」后被降级回历史快照（382/498），导致联系人菜单消失、招生分析 403 —— 在 `applyToEngine` / `persist` 收口处自愈（`cf9d5b0`）

### 2026-09-13
- **教学域三模块上线**（`73b09a0`）：成绩册 / 课程规划 / 行为记录（21 张新表）
- **14 个菜单补齐权限点 + 存量角色迁移**（`0f1c036`）—— 教学管理 / AI 路由菜单首次可授权
- 部门管理改造：修「公司」根节点不显示、新增部门成员快照（点部门看员工）
- 修复 `SqlStore.search` 返回 `recordId` 的通用陷阱（`2a3a1d9`）—— 影响 3 处
- 卫瓴联系人：筛选下拉改取实际值（不再显示数字编码）、列表瘦身、招生分析 KPI 下钻（`41d4661`、`e05e367`）

### 2026-09-12
- **AI 路由上线**（`4e7b368`、`4697c24`）：从 acapi/sub2api 移植网关全套（6 模块 + `/v1` 转发）
- 招生分析新增流失分析维度、跟进分析；学生类报表支持下钻

### 更早（2026-08-25 ~ 09-11）
- 飞书 → PostgreSQL 数据层迁移（79 表全量切读、1838 个附件全部本地化）
- Blue-Green 零停机部署上线
- 角色权限体系（模块 × 动作矩阵 + 字段密级脱敏 + 审计四件套）
- 邮件自动归档模块（多账号 IMAP + AES 加密凭证 + 附件本地化 + 关联学生）
- 全站国际化（zh/en）+ i18n 构建期 lint
- 部门管理、系统监控、登录日志、活跃时段报表

> 完整执行记录（673 条）见 [`docs/TASKS-ARCHIVE.md`](docs/TASKS-ARCHIVE.md)。

---

## 五、统计

> 两张表都是**可核对**的：计划条目逐条在第二节，执行任务逐条在 `docs/TASKS-ARCHIVE.md`。
> 每次同步请顺手数一遍（此前出现过「表格已 55 条、统计还写 42」的滞后）。

| 指标 | 数值 |
|---|---|
| 计划条目 | 81（已完成 78 · 待开始 3）—— 见第二节各域表格（基础设施 22 · 招生管理 22 · 权限安全 16 · 教学管理 12 · 报表管理 5 · AI 路由 3 · 数据质量 1） |
| 待办（**Issues 是真源**） | 6 项 open：[#1](https://github.com/sunxufeng/acms/issues/1) 教学班数据补录（P0·阻塞）· [#3](https://github.com/sunxufeng/acms/issues/3) AI 路由用户管理 · [#4](https://github.com/sunxufeng/acms/issues/4) 首页 logo 重传 · [#12](https://github.com/sunxufeng/acms/issues/12) 关联笔记可见性口径 · [#13](https://github.com/sunxufeng/acms/issues/13) 会议纪要加关联学生 · [#14](https://github.com/sunxufeng/acms/issues/14) 两处字典与实际数据不匹配 |
| 导出副本 `PLAN.csv` | 103 条（已完成 100 · 待开始 3）—— 与第二节**同源但更细**（含逐条修复），供工作台「项目 → 计划」导入 |
| 执行任务（8/25 – 9/24） | 953（已完成 950 · 待处理 3）—— 见 `docs/TASKS-ARCHIVE.md` |
| 工作日 | 29 天（8/25 起算，含两端自然日） |
| 线上部署形态 | Blue-Green，当前活跃 slot 见服务器 `/opt/acms/repo/.deploy_slot` |

---

## 六、协作约定

1. **本文件是唯一真源**：计划变更（新增条目、调整优先级、推进状态）都改这里，然后提交。
2. **状态口径**：代码合并 + 部署上线 + 线上验证通过，才算 `已完成`；只写完代码未上线为 `进行中`。
3. **待办项流转**：进入开发前，把该条目在项目管理工具里建成事项、指派负责人、挂上本文件与 PRD。
4. **AI 助手同步义务**：每次交付后更新本文件的对应行（状态 / 备注里的 commit），并在 `docs/TASKS-ARCHIVE.md` 追加当日执行记录。
5. **上线纪律**：部署生产与推送 GitHub 需先确认；改动后必须线上验证。

### 相关文档
- [架构说明](docs/ARCHITECTURE.md) · [部署说明](docs/deploy.md) · [PG 迁移方案](docs/postgres-migration-plan.md)
- [学生门户方案](docs/student-portal-plan.md)（门户待办的设计底稿）
- [任务归档](docs/TASKS-ARCHIVE.md)
